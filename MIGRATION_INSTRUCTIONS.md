# Инструкция по применению миграции для шаблонов писем

## 🖥️ На локальном ПК (для разработки)

### Вариант 1: Через npm (рекомендуется)
```bash
npm run db:migrate
```

### Вариант 2: Через npx напрямую
```bash
npx prisma migrate dev --name add_email_templates
```

Это создаст файл миграции и применит его к вашей локальной базе данных.

---

## ☁️ На Vercel (для продакшена)

### Вариант 1: Через Vercel CLI (рекомендуется)

1. **Установите Vercel CLI** (если еще не установлен):
   ```bash
   npm i -g vercel
   ```

2. **Войдите в Vercel**:
   ```bash
   vercel login
   ```

3. **Подключите проект**:
   ```bash
   vercel link
   ```

4. **Получите переменные окружения**:
   ```bash
   vercel env pull .env.local
   ```

5. **Примените миграции**:
   ```bash
   npm run db:deploy
   ```
   или
   ```bash
   npx prisma migrate deploy
   ```

### Вариант 2: Через Vercel Dashboard

1. Зайдите в настройки проекта на Vercel
2. Перейдите в раздел **Settings → Environment Variables**
3. Убедитесь, что `DATABASE_URL` настроен правильно
4. В разделе **Deployments** найдите последний деплой
5. Откройте **Build Logs** и проверьте, что миграции применяются

### Вариант 3: Автоматически через Build Command

Если вы хотите, чтобы миграции применялись автоматически при каждом деплое, добавьте в настройках проекта Vercel:

**Build Command:**
```bash
npm run db:generate && npm run db:deploy && npm run build
```

⚠️ **Внимание:** Это может замедлить процесс деплоя, но гарантирует, что миграции всегда применятся.

---

## 📝 Проверка миграции

После применения миграции проверьте:

1. **Локально:**
   ```bash
   npx prisma studio
   ```
   Откройте браузер и проверьте, что таблица `EmailTemplate` появилась.

2. **На Vercel:**
   - Зайдите в админ-панель вашего приложения
   - Перейдите в раздел "Шаблоны писем" (должен быть доступен для админов)
   - Попробуйте создать тестовый шаблон

---

## ⚠️ Важные замечания

1. **Всегда сначала тестируйте миграции локально** перед применением на продакшене
2. **Сделайте backup базы данных** перед применением миграций на продакшене
3. **Используйте `prisma migrate deploy`** для продакшена (не `migrate dev`)
4. **Убедитесь, что `DATABASE_URL`** правильно настроен в переменных окружения Vercel

---

## 🔧 Решение проблем

### Ошибка: "Migration engine failed to connect to database"
- Проверьте, что `DATABASE_URL` правильно настроен
- Убедитесь, что база данных доступна из сети

### Ошибка: "Migration already applied"
- Это нормально, миграция уже применена
- Проверьте статус: `npx prisma migrate status`

### Ошибка на Vercel: "Command not found: prisma"
- Убедитесь, что `@prisma/client` в зависимостях (не в devDependencies)
- Проверьте, что `postinstall` скрипт выполняется (он есть в package.json)

