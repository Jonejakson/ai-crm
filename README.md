# CRM System с AI Ассистентом

Современная CRM система с интегрированным AI ассистентом на базе Next.js, Prisma и OpenAI.

## Возможности

- 📊 **Дашборд** - обзор статистики и последних клиентов
- 👥 **Управление клиентами** - добавление, редактирование, удаление контактов
- ✅ **Задачи** - создание и отслеживание задач с привязкой к клиентам
- 💬 **Диалоги** - история общения с клиентами
- 🤖 **AI Ассистент** - интеллектуальный помощник для работы с CRM данными

## Технологии

- **Next.js 16** - React фреймворк
- **Prisma** - ORM для работы с базой данных
- **PostgreSQL** - база данных (для продакшена)
- **SQLite** - база данных (для разработки, опционально)
- **OpenAI API** - для AI ассистента
- **Tailwind CSS** - стилизация

## 🚀 Деплой

Подробные инструкции по деплою см. в [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)

**Быстрый деплой на Vercel:**
1. Подключите репозиторий к Vercel
2. Добавьте переменные окружения из `.env.example`
3. Настройте PostgreSQL базу данных
4. Примените миграции: `npm run db:deploy`

## Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

#### Для разработки (SQLite - опционально)

Если хотите использовать SQLite для разработки, временно измените `prisma/schema.prisma`:

```prisma
datasource db {
    provider = "sqlite"
    url      = "file:./dev.db"
}
```

#### Для продакшена (PostgreSQL) - Рекомендуется

**Подробная инструкция:** см. `docs/POSTGRESQL_MIGRATION.md`

**Быстрый старт:**

1. **Установите PostgreSQL** (см. инструкцию выше)

2. **Создайте базу данных:**
   ```bash
   psql -U postgres
   CREATE DATABASE crm_db;
   \q
   ```

3. **Настройте переменную окружения `DATABASE_URL`** в `.env`:
   ```env
   DATABASE_URL="postgresql://postgres:your_password@localhost:5432/crm_db?schema=public"
   ```

4. **Выполните миграции:**
   ```bash
   # Применение миграций
   npm run db:migrate:postgres
   
   # Или вручную
   npx prisma migrate dev --name init_postgresql
   npx prisma generate
   ```

5. **(Опционально) Миграция данных из SQLite:**
   ```bash
   node scripts/migrate-to-postgresql.js
   ```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```env
# Database
DATABASE_URL="postgresql://user:password@localhost:5432/crm_db?schema=public"

# NextAuth Secret (обязательно для аутентификации)
# Сгенерируйте случайную строку: openssl rand -base64 32
AUTH_SECRET=your_secret_key_here
NEXTAUTH_URL=http://localhost:3000

# OpenAI API Key (опционально, для работы AI ассистента)
# Получите ключ на https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Encryption Key (обязательно для шифрования чувствительных данных)
# Сгенерируйте: openssl rand -hex 32
ENCRYPTION_KEY=your_encryption_key_64_chars_hex

# Node Environment
NODE_ENV=development
```

**Важно:**
- `AUTH_SECRET` (или `NEXTAUTH_SECRET`) обязателен для работы аутентификации. Сгенерируйте случайную строку командой: `openssl rand -base64 32`
- Если не указать `OPENAI_API_KEY`, AI ассистент будет работать в демо-режиме с базовыми ответами.

### 4. Запуск приложения

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Использование

### AI Ассистент

AI ассистент доступен через кнопку в боковой панели. Он может:
- Анализировать данные CRM
- Отвечать на вопросы о клиентах и задачах
- Предоставлять рекомендации
- Работать с контекстом конкретного клиента (при просмотре страницы клиента)

**Примеры вопросов:**
- "Какие клиенты требуют внимания?"
- "Какие ближайшие задачи?"
- "Покажи статистику"
- "Что известно об этом клиенте?" (на странице клиента)

### Управление данными

- **Клиенты**: Добавление, редактирование, удаление контактов
- **Задачи**: Создание задач с привязкой к клиентам, изменение статусов
- **Диалоги**: Отправка сообщений клиентам, просмотр истории

## Структура проекта

```
my-app/
├── app/                    # Next.js App Router
│   ├── api/               # API routes
│   ├── contacts/          # Страницы клиентов
│   ├── tasks/             # Страницы задач
│   └── dialogs/           # Страницы диалогов
├── components/            # React компоненты
├── lib/                   # Утилиты и конфигурация
├── prisma/                # Prisma schema и миграции
└── public/                # Статические файлы
```

## Разработка

```bash
# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшен версии
npm start

# Линтинг
npm run lint
```

## База данных

Проект настроен на использование **PostgreSQL** для продакшена. 

**Текущая конфигурация в `prisma/schema.prisma`:**
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

**Для миграции на PostgreSQL:**
1. См. подробную инструкцию: `docs/POSTGRESQL_MIGRATION.md`
2. Настройте `DATABASE_URL` в `.env`
3. Выполните: `npm run db:migrate:postgres`

**Полезные команды:**
- `npm run db:migrate` - создать и применить миграцию
- `npm run db:generate` - сгенерировать Prisma Client
- `npm run db:studio` - открыть Prisma Studio
- `npm run db:deploy` - применить миграции в продакшене

## Деплой

Проект готов к деплою на Vercel, Netlify или любой другой платформе, поддерживающей Next.js.

Не забудьте:
- Настроить переменные окружения
- Выполнить миграции базы данных
- Указать `OPENAI_API_KEY` для работы AI

## Лицензия

MIT
