# 🚀 Быстрый старт: Деплой CRM системы

## ⚡ TL;DR - Что делать прямо сейчас

### Если у вас VPS Beget:
1. Следуйте инструкции: `docs/BEGET_DEPLOYMENT.md` (Вариант 1)
2. Нужно: SSH доступ, Node.js 20, PostgreSQL, PM2

### Если у вас виртуальный хостинг Beget:
**Виртуальный хостинг НЕ поддерживает Node.js!** Используйте альтернативу:

---

## 🎯 Рекомендуемый вариант: Vercel + Neon (БЕСПЛАТНО)

### Шаг 1: Подготовка кода (5 минут)

```bash
# В PowerShell в папке проекта
cd C:\ai_crm\my-app

# Инициализируйте Git (если еще не сделано)
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub и выполните:
git remote add origin https://github.com/ВАШ_ЛОГИН/ВАШ_РЕПО.git
git push -u origin main
```

### Шаг 2: Создайте базу данных PostgreSQL (2 минуты)

1. Зайдите на https://neon.tech
2. Зарегистрируйтесь (можно через GitHub)
3. Создайте новый проект
4. Скопируйте **Connection String** (DATABASE_URL)

### Шаг 3: Деплой на Vercel (5 минут)

1. Зайдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий
5. В настройках добавьте **Environment Variables**:

```
DATABASE_URL=postgresql://... (из Neon)
AUTH_SECRET=сгенерируйте_32_символа
NEXTAUTH_URL=https://ваш-проект.vercel.app
OPENAI_API_KEY=ваш_ключ (опционально)
```

6. Нажмите **Deploy**

### Шаг 4: Применить миграции БД (2 минуты)

```bash
# Установите Vercel CLI
npm i -g vercel

# В папке проекта
vercel login
vercel link
vercel env pull .env.local

# Примените миграции
npm run db:deploy
```

**Готово!** Ваше приложение доступно по адресу: `https://ваш-проект.vercel.app`

---

## 📊 Сравнение вариантов

| Вариант | Стоимость | Сложность | Подходит для |
|---------|-----------|-----------|--------------|
| **Vercel + Neon** | Бесплатно | ⭐ Легко | ✅ Рекомендуется |
| **Railway** | Бесплатно ($5/мес) | ⭐⭐ Средне | ✅ Хорошо |
| **Beget VPS** | Платно | ⭐⭐⭐ Сложно | ✅ Если уже оплачен |
| **Render** | Бесплатно | ⭐⭐ Средне | ⚠️ Засыпает через 15 мин |

---

## 🔑 Генерация AUTH_SECRET

**Windows PowerShell:**
```powershell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
```

**Или онлайн:** https://generate-secret.vercel.app/32

---

## ✅ Чеклист перед деплоем

- [ ] Код загружен на GitHub
- [ ] Создана PostgreSQL база данных
- [ ] Сгенерирован AUTH_SECRET
- [ ] Все переменные окружения готовы
- [ ] Локально проект собирается: `npm run build`
- [ ] Миграции применены: `npm run db:deploy`

---

## 📚 Подробные инструкции

- **Деплой на Beget VPS:** `docs/BEGET_DEPLOYMENT.md`
- **Общая документация:** `docs/DEPLOYMENT.md`
- **Настройка переменных:** `docs/ENV_SETUP.md`

---

## 🆘 Быстрая помощь

**Проблема:** Приложение не запускается
→ Проверьте переменные окружения, особенно `DATABASE_URL` и `AUTH_SECRET`

**Проблема:** Ошибка подключения к БД
→ Проверьте строку подключения, убедитесь что БД доступна извне (для Vercel)

**Проблема:** Ошибка миграций
→ Выполните: `npm run db:generate && npm run db:deploy`

---

## 💡 Совет

**Для первого деплоя используйте Vercel** - это самый простой и быстрый способ. Потом, если нужно, можно перенести на Beget VPS.

