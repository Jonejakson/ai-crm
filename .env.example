# Скопируйте в .env и заполните значения.
# На сервере (flamecrm.ru) обязательно задайте NEXTAUTH_URL и секреты.

# Публичный URL приложения (для редиректов из писем и OAuth)
NEXTAUTH_URL=https://flamecrm.ru

# База данных
DATABASE_URL=postgresql://crm_user:YOUR_PASSWORD@postgres:5432/crm_db

# NextAuth
NEXTAUTH_SECRET=your-secret-at-least-32-chars
AUTH_SECRET=your-secret-at-least-32-chars

# Postgres (для docker-compose)
POSTGRES_PASSWORD=change_this_password

# Опционально: почта, DaData, YooKassa, S3, ENCRYPTION_KEY и т.д. — см. docker-compose.yml
