# PWA (Progressive Web App) - Инструкция

## Что реализовано

✅ Web App Manifest (`/public/manifest.json`)
✅ Service Worker (`/public/service-worker.js`)
✅ Офлайн страница (`/app/offline/page.tsx`)
✅ Компонент регистрации PWA (`/components/PWARegister.tsx`)
✅ PWA мета-теги в layout

## Создание иконок

### Вариант 1: Использование скрипта (рекомендуется)

1. Установите sharp:
```bash
npm install sharp
```

2. Запустите скрипт:
```bash
node scripts/generate-icons.js
```

### Вариант 2: Онлайн-конвертер

1. Откройте https://realfavicongenerator.net/
2. Загрузите файл `public/icon.svg`
3. Скачайте сгенерированные иконки
4. Поместите их в папку `public/` с именами:
   - `icon-16x16.png`
   - `icon-32x32.png`
   - `icon-72x72.png`
   - `icon-96x96.png`
   - `icon-128x128.png`
   - `icon-144x144.png`
   - `icon-152x152.png`
   - `icon-192x192.png`
   - `icon-384x384.png`
   - `icon-512x512.png`

### Вариант 3: Ручное создание

Создайте PNG иконки размером 512x512px с логотипом Pocket CRM и конвертируйте их в нужные размеры.

## Проверка PWA

### Chrome DevTools

1. Откройте DevTools (F12)
2. Перейдите на вкладку "Application"
3. Проверьте:
   - Manifest - должен быть загружен
   - Service Workers - должен быть зарегистрирован
   - Cache Storage - должны быть созданы кэши

### Lighthouse

1. Откройте DevTools (F12)
2. Перейдите на вкладку "Lighthouse"
3. Выберите "Progressive Web App"
4. Запустите проверку

### Установка на устройство

#### Android (Chrome)
1. Откройте сайт в Chrome
2. Нажмите меню (три точки)
3. Выберите "Установить приложение" или "Добавить на главный экран"

#### iOS (Safari)
1. Откройте сайт в Safari
2. Нажмите кнопку "Поделиться"
3. Выберите "На экран «Домой»"

#### Desktop (Chrome/Edge)
1. В адресной строке появится иконка установки
2. Или используйте меню → "Установить Pocket CRM"

## Функции PWA

### ✅ Реализовано

- Кэширование статических ресурсов
- Офлайн режим (частичный)
- Установка на устройство
- Быстрый доступ с домашнего экрана
- Адаптивный дизайн

### 🔄 В разработке

- Push-уведомления (базовая структура готова)
- Фоновое обновление данных
- Синхронизация при восстановлении связи

## Обновление Service Worker

При изменении Service Worker:

1. Обновите версию кэша в `service-worker.js`:
```javascript
const CACHE_NAME = 'pocket-crm-v2'; // Увеличьте версию
```

2. Перезагрузите страницу дважды (первый раз - установка, второй - активация)

3. Или используйте DevTools → Application → Service Workers → "Update"

## Отладка

### Просмотр кэша
1. DevTools → Application → Cache Storage
2. Проверьте содержимое кэшей

### Очистка кэша
1. DevTools → Application → Clear storage
2. Или программно: `caches.delete('pocket-crm-v1')`

### Логи Service Worker
1. DevTools → Application → Service Workers
2. Нажмите "inspect" для просмотра консоли

## Требования

- HTTPS (обязательно для PWA в production)
- Service Worker API (поддерживается во всех современных браузерах)
- Web App Manifest (поддерживается во всех современных браузерах)

## Производительность

Service Worker кэширует:
- Статические ресурсы (CSS, JS, изображения)
- HTML страницы
- API ответы (для офлайн режима)

Стратегии кэширования:
- **Cache First**: для статических ресурсов
- **Network First**: для API запросов и HTML
- **Offline Fallback**: офлайн страница при отсутствии сети

