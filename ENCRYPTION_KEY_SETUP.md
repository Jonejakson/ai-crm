# 🔐 Настройка ENCRYPTION_KEY - Пошаговая инструкция

## Что такое ENCRYPTION_KEY?

`ENCRYPTION_KEY` - это секретный ключ для шифрования чувствительных данных в вашей CRM:
- Пароли email интеграций (IMAP/SMTP)
- OAuth токены (Gmail, Outlook)
- API ключи (Telegram, WhatsApp, рекламные платформы)
- Пароли систем учета (МойСклад, 1С)

**ВАЖНО:** Без этого ключа невозможно расшифровать данные! Храните его в безопасном месте.

---

## Шаг 1: Генерация ключа

### Вариант 1: Windows PowerShell (рекомендуется)

Откройте PowerShell и выполните:

```powershell
# Генерируем 32 байта случайных данных и конвертируем в hex (64 символа)
-join ((1..32 | ForEach-Object { '{0:X2}' -f (Get-Random -Minimum 0 -Maximum 256) }))
```

**Пример результата:**
```
A1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2
```

### Вариант 2: Онлайн генератор

1. Откройте: https://generate-secret.vercel.app/64
2. Скопируйте сгенерированный ключ (64 символа)

### Вариант 3: Node.js (если установлен)

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

---

## Шаг 2: Добавление ключа локально (для разработки)

1. **Откройте файл `.env` в корне проекта**

   Если файла нет, создайте его:
   ```bash
   # В корне проекта создайте файл .env
   ```

2. **Добавьте строку:**
   ```env
   ENCRYPTION_KEY=ваш_сгенерированный_ключ_64_символа
   ```

   **Пример:**
   ```env
   ENCRYPTION_KEY=A1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2
   ```

3. **Сохраните файл**

4. **Перезапустите приложение:**
   ```bash
   npm run dev
   ```

---

## Шаг 3: Добавление ключа в Vercel (для production)

### Способ 1: Через веб-интерфейс Vercel

1. **Зайдите на https://vercel.com**
2. **Выберите ваш проект**
3. **Перейдите в Settings → Environment Variables**
4. **Нажмите "Add New"**
5. **Заполните:**
   - **Key:** `ENCRYPTION_KEY`
   - **Value:** ваш сгенерированный ключ (64 символа)
   - **Environment:** выберите `Production`, `Preview`, `Development` (или все)
6. **Нажмите "Save"**
7. **Передеплойте проект:**
   - Перейдите в Deployments
   - Нажмите "..." на последнем деплое
   - Выберите "Redeploy"

### Способ 2: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Добавьте переменную окружения
vercel env add ENCRYPTION_KEY production

# Введите значение ключа, когда попросит
```

---

## Шаг 4: Проверка

### Локально

1. Запустите приложение:
   ```bash
   npm run dev
   ```

2. Проверьте консоль - не должно быть предупреждения:
   ```
   ⚠️  ENCRYPTION_KEY не установлен...
   ```

3. Если видите предупреждение - проверьте:
   - Файл `.env` существует
   - Ключ добавлен правильно (без пробелов, кавычек)
   - Приложение перезапущено

### В Production (Vercel)

1. Зайдите в Vercel → ваш проект → Settings → Environment Variables
2. Убедитесь, что `ENCRYPTION_KEY` есть в списке
3. Проверьте логи деплоя - не должно быть предупреждений

---

## Важные правила безопасности

### ✅ ДЕЛАТЬ:
- ✅ Использовать разные ключи для development и production
- ✅ Хранить ключ в безопасном месте (менеджер паролей)
- ✅ Использовать переменные окружения (не хардкодить в код)
- ✅ Регулярно делать резервные копии ключа

### ❌ НЕ ДЕЛАТЬ:
- ❌ НЕ коммитить `.env` файл в Git
- ❌ НЕ передавать ключ по незащищенным каналам (email, мессенджеры)
- ❌ НЕ использовать один ключ для всех окружений
- ❌ НЕ хранить ключ в открытом виде

---

## Что делать, если ключ потерян?

**К сожалению, если ключ потерян:**
- ❌ Невозможно расшифровать зашифрованные данные
- ❌ Нужно будет заново ввести все пароли/токены в интеграциях

**Решение:**
1. Сгенерируйте новый ключ
2. Добавьте его в `.env` и Vercel
3. Обновите все интеграции вручную (введите пароли/токены заново)

---

## Проверка текущего состояния

Чтобы проверить, установлен ли ключ:

### Локально:
```bash
# Проверьте содержимое .env файла
cat .env | grep ENCRYPTION_KEY
```

### В Vercel:
1. Зайдите в Settings → Environment Variables
2. Найдите `ENCRYPTION_KEY` в списке

---

## Пример полного .env файла

```env
# Database
DATABASE_URL="postgresql://user:password@localhost:5432/crm_db?schema=public"

# NextAuth
AUTH_SECRET=your_auth_secret_here
NEXTAUTH_URL=http://localhost:3000

# Encryption Key (64 символа hex)
ENCRYPTION_KEY=A1B2C3D4E5F6G7H8I9J0K1L2M3N4O5P6Q7R8S9T0U1V2W3X4Y5Z6A7B8C9D0E1F2

# OpenAI (опционально)
OPENAI_API_KEY=your_openai_key_here

# YooKassa (опционально)
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
```

---

## Устранение проблем

### Проблема: "ENCRYPTION_KEY не установлен"

**Решение:**
1. Проверьте, что ключ добавлен в `.env` (локально) или Vercel (production)
2. Убедитесь, что нет пробелов или лишних символов
3. Перезапустите приложение

### Проблема: "Ошибка расшифровки данных"

**Причины:**
- Неверный ключ
- Данные были зашифрованы другим ключом

**Решение:**
- Проверьте правильность ключа
- Если ключ был изменен, обновите все интеграции вручную

---

**Дата создания:** 2024-12-XX  
**Версия:** 1.0

