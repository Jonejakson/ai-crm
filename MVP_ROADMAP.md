# 🚀 План доработки CRM до MVP (по примеру amoCRM)

## Текущее состояние ✅

**Что уже есть:**
- ✅ Управление клиентами (CRUD)
- ✅ Задачи с привязкой к клиентам
- ✅ Диалоги/сообщения
- ✅ AI Ассистент (OpenAI интеграция)
- ✅ Базовый дашборд
- ✅ SQLite база данных

---

## 📋 План доработки (приоритетный порядок)

### 🔴 КРИТИЧНО для MVP (1-2 недели)

#### 1. **Аутентификация и авторизация** (2-3 дня)
**Зачем:** Безопасность, многопользовательская работа
- [ ] Установить NextAuth.js
- [ ] Создать модель User в Prisma
- [ ] Страницы логина/регистрации
- [ ] Защита API routes
- [ ] Сессии и middleware

**Технологии:** NextAuth.js, bcrypt, JWT

---

#### 2. **Сделки (Deals) с воронками** (3-4 дня)
**Зачем:** Основная функция CRM - управление продажами
- [ ] Модель Deal в Prisma (сумма, этап, ответственный)
- [ ] Модель Pipeline (воронка продаж)
- [ ] Страница сделок с канбан-доской
- [ ] Перетаскивание между этапами
- [ ] Привязка к клиентам

**Структура:**
```
Deal {
  - title, amount, currency
  - stage (этап воронки)
  - contactId, userId
  - expectedCloseDate
  - probability (%)
}
```

---

#### 3. **Улучшенный поиск и фильтры** (2 дня)
**Зачем:** Быстрый доступ к данным
- [ ] Глобальный поиск (клиенты, сделки, задачи)
- [ ] Расширенные фильтры
- [ ] Сохраненные фильтры
- [ ] Сортировка

---

#### 4. **Календарь и события** (2-3 дня)
**Зачем:** Планирование встреч и звонков
- [ ] Модель Event в Prisma
- [ ] Календарный вид
- [ ] Создание событий из задач/сделок
- [ ] Напоминания

---

### 🟡 ВАЖНО для MVP (1 неделя)

#### 5. **Базовая аналитика** (2-3 дня)
**Зачем:** Понимание эффективности
- [ ] Графики продаж
- [ ] Конверсия по воронкам
- [ ] Статистика по клиентам
- [ ] Отчеты по задачам

**Библиотеки:** Recharts или Chart.js

---

#### 6. **Улучшение UI/UX** (2-3 дня)
**Зачем:** Профессиональный вид
- [ ] Современный дизайн (как amoCRM)
- [ ] Адаптивность (мобильная версия)
- [ ] Темная тема (опционально)
- [ ] Улучшенные формы и модальные окна
- [ ] Загрузка состояний (skeletons)

**Библиотеки:** shadcn/ui или Headless UI

---

#### 7. **Уведомления** (1-2 дня)
**Зачем:** Не пропустить важное
- [ ] Уведомления о просроченных задачах
- [ ] Уведомления о новых сделках
- [ ] Бейджи с количеством
- [ ] Центр уведомлений

---

### 🟢 ЖЕЛАТЕЛЬНО (опционально)

#### 8. **Экспорт данных** (1 день)
- [ ] Экспорт клиентов в CSV/Excel
- [ ] Экспорт сделок
- [ ] Экспорт отчетов

#### 9. **Миграция на PostgreSQL** (1 день)
- [ ] Настройка PostgreSQL
- [ ] Миграция данных
- [ ] Обновление Prisma schema

#### 10. **Дополнительные функции**
- [ ] Теги/метки для клиентов
- [ ] Файлы и документы
- [ ] История изменений (аудит)
- [ ] Комментарии к сделкам

---

## 🛠 Технологический стек (дополнения)

### Новые зависимости:
```json
{
  "next-auth": "^5.0.0",           // Аутентификация
  "bcryptjs": "^2.4.3",            // Хеширование паролей
  "recharts": "^2.10.0",          // Графики
  "react-beautiful-dnd": "^13.1.1", // Drag & Drop для канбана
  "date-fns": "^3.0.0",            // Работа с датами
  "react-calendar": "^4.6.0",     // Календарь
  "xlsx": "^0.18.5",              // Экспорт Excel
  "@radix-ui/react-*": "...",      // UI компоненты (опционально)
  "zod": "^3.22.0"                 // Валидация
}
```

---

## 📊 Структура базы данных (расширенная)

```prisma
model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String
  password  String
  role      String   @default("user")
  deals     Deal[]
  tasks     Task[]
  events    Event[]
}

model Deal {
  id              Int      @id @default(autoincrement())
  title           String
  amount          Float
  currency        String   @default("RUB")
  stage           String   // этап воронки
  probability     Int      @default(0) // 0-100%
  expectedCloseDate DateTime?
  contact         Contact  @relation(fields: [contactId], references: [id])
  contactId       Int
  user            User     @relation(fields: [userId], references: [id])
  userId          Int
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

model Pipeline {
  id          Int      @id @default(autoincrement())
  name        String
  stages      String   // JSON массив этапов
  isDefault   Boolean  @default(false)
}

model Event {
  id          Int      @id @default(autoincrement())
  title       String
  description String?
  startDate  DateTime
  endDate     DateTime?
  type        String   // meeting, call, task
  contact     Contact? @relation(fields: [contactId], references: [id])
  contactId   Int?
  user        User     @relation(fields: [userId], references: [id])
  userId      Int
  createdAt   DateTime @default(now())
}
```

---

## 🚀 Подготовка к деплою

### 1. Переменные окружения (.env.production)
```env
DATABASE_URL="postgresql://..."
NEXTAUTH_URL="https://yourdomain.com"
NEXTAUTH_SECRET="..."
OPENAI_API_KEY="..."
NODE_ENV="production"
```

### 2. Оптимизация сборки
- [ ] Проверка размера бандла
- [ ] Оптимизация изображений
- [ ] Lazy loading компонентов
- [ ] Кэширование API routes

### 3. Платформы для деплоя
- **Vercel** (рекомендуется для Next.js)
- **Railway** (с PostgreSQL)
- **DigitalOcean App Platform**
- **AWS/GCP** (для масштабирования)

---

## 📅 Временные рамки

**Минимальный MVP (2-3 недели):**
- Аутентификация
- Сделки с воронками
- Поиск и фильтры
- Базовый UI/UX
- Деплой

**Полноценный MVP (4-5 недель):**
- Все выше + Календарь, Аналитика, Уведомления

---

## 🎯 Критерии готовности MVP

✅ Работает аутентификация
✅ Можно создавать и управлять сделками
✅ Есть воронки продаж
✅ Работает поиск
✅ AI ассистент функционирует
✅ Адаптивный дизайн
✅ Развернуто на сервере
✅ База данных PostgreSQL
✅ Базовые тесты работают

---

## 📝 Следующие шаги

1. **Начать с аутентификации** - это основа для всего остального
2. **Добавить сделки** - ключевая функция CRM
3. **Улучшить UI** - чтобы выглядело профессионально
4. **Подготовить к деплою** - настроить PostgreSQL и переменные окружения

Готов начать с любого пункта! 🚀




