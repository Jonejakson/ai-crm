# 📦 Инструкции по экспорту проекта

## Вариант 1: Использование Git (Рекомендуется)

Проект уже находится в Git репозитории, поэтому самый простой способ получить актуальную версию:

### Получить актуальную версию:
```bash
git clone https://github.com/Jonejakson/ai-crm.git
cd ai-crm
git checkout main
```

### Создать архив из Git:
```bash
# Создать архив текущей версии
git archive --format=zip --output=ai-crm-export.zip HEAD

# Или создать архив с определенным коммитом
git archive --format=zip --output=ai-crm-export.zip 66c8e40
```

---

## Вариант 2: Ручное создание архива

Если нужно создать архив вручную, исключите следующие папки/файлы:
- `node_modules/` - зависимости (можно установить через `npm install`)
- `.next/` - сборочные файлы Next.js
- `prisma/*.db` - локальные базы данных
- `.env*` - файлы с секретами
- `.vercel/` - конфигурация Vercel
- `coverage/` - отчеты тестов

### PowerShell команда для создания архива:
```powershell
# Перейти в родительскую директорию
cd C:\ai_crm

# Создать архив, исключая ненужные папки
$exclude = @('node_modules', '.next', 'prisma\*.db', '.env*', '.vercel', 'coverage')
Compress-Archive -Path "my-app\*" -DestinationPath "ai-crm-export.zip" -Force
```

---

## Вариант 3: Использование .gitignore

Проект уже настроен с правильным `.gitignore`. Все важные файлы находятся в репозитории.

### Что включено в репозиторий:
✅ Весь исходный код  
✅ Документация  
✅ Конфигурационные файлы  
✅ Prisma схемы и миграции  
✅ Компоненты и утилиты  

### Что НЕ включено (и правильно):
❌ `node_modules/` - устанавливается через `npm install`  
❌ `.env` - создается локально с секретами  
❌ Базы данных - создаются локально  
❌ Сборочные файлы - генерируются при сборке  

---

## 📋 Чеклист для экспорта

Перед экспортом убедитесь, что у вас есть:

1. ✅ **Исходный код** - все файлы в `app/`, `components/`, `lib/`, и т.д.
2. ✅ **Документация** - все `.md` файлы
3. ✅ **Конфигурация** - `package.json`, `tsconfig.json`, `next.config.ts`, и т.д.
4. ✅ **Prisma схемы** - `prisma/schema.prisma` и миграции
5. ✅ **План работы** - `WORK_PLAN_SUMMARY.md` (создан)

---

## 🚀 После получения проекта

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Настройте базу данных:**
   - Создайте PostgreSQL базу
   - Настройте `DATABASE_URL` в `.env`
   - Выполните миграции: `npm run db:migrate`

3. **Настройте переменные окружения:**
   - См. `README.md` для списка переменных
   - См. `docs/ENV_SETUP.md` для подробностей

4. **Запустите проект:**
   ```bash
   npm run dev
   ```

---

## 📚 Документация в проекте

После получения проекта, начните с:
1. `README.md` - основная документация
2. `WORK_PLAN_SUMMARY.md` - **сводный план работы** (все планы в одном месте)
3. `EXPORT_INFO.md` - информация об экспорте

---

**Примечание:** Проект уже находится в Git, поэтому самый надежный способ получить актуальную версию - использовать `git clone` или `git archive`.


