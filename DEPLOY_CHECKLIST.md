# Чеклист деплоя на Selectel

Используйте этот чеклист для отслеживания прогресса деплоя.

## ✅ Подготовка сервера

- [ ] Подключение к серверу по SSH
- [ ] Обновление системы: `sudo apt update && sudo apt upgrade -y`
- [ ] Установка Docker: `sudo apt install -y docker.io docker-compose`
- [ ] Запуск Docker: `sudo systemctl enable docker && sudo systemctl start docker`
- [ ] Добавление пользователя в группу docker: `sudo usermod -aG docker $USER`
- [ ] Переподключение к серверу (для применения изменений группы)

## ✅ Клонирование проекта

- [ ] Переход в директорию: `cd /opt`
- [ ] Клонирование репозитория: `git clone https://github.com/Jonejakson/ai-crm.git`
- [ ] Переход в директорию проекта: `cd ai-crm/my-app`
- [ ] Проверка наличия файлов: `ls -la` (должны быть Dockerfile, docker-compose.yml)

## ✅ Настройка переменных окружения

- [ ] Создание файла `.env`: `nano .env`
- [ ] Заполнение обязательных переменных:
  - [ ] `POSTGRES_PASSWORD` - надежный пароль для PostgreSQL
  - [ ] `NEXTAUTH_URL` - URL вашего домена (https://your-domain.com)
  - [ ] `NEXTAUTH_SECRET` - секретный ключ (можно сгенерировать: `openssl rand -base64 32`)
- [ ] Заполнение опциональных переменных (SMTP, DaData, Sentry)
- [ ] Сохранение файла

## ✅ Запуск приложения

- [ ] Запуск скрипта деплоя: `chmod +x scripts/deploy-selectel.sh && ./scripts/deploy-selectel.sh`
  - ИЛИ вручную:
  - [ ] Сборка образов: `docker-compose build`
  - [ ] Запуск контейнеров: `docker-compose up -d`
  - [ ] Проверка статуса: `docker-compose ps`
  - [ ] Применение миграций: `docker-compose exec app npx prisma migrate deploy`
  - [ ] Генерация Prisma: `docker-compose exec app npx prisma generate`
- [ ] Проверка логов: `docker-compose logs -f app`
- [ ] Проверка доступности: `curl http://localhost:3000` (должен вернуть HTML)

## ✅ Настройка Nginx

- [ ] Установка Nginx: `sudo apt install -y nginx`
- [ ] Создание конфигурации: `sudo nano /etc/nginx/sites-available/crm`
- [ ] Копирование конфигурации из DEPLOY_SELECTEL.md
- [ ] Замена `your-domain.com` на ваш домен
- [ ] Активация конфигурации: `sudo ln -s /etc/nginx/sites-available/crm /etc/nginx/sites-enabled/`
- [ ] Проверка конфигурации: `sudo nginx -t`
- [ ] Перезапуск Nginx: `sudo systemctl restart nginx`
- [ ] Проверка доступности через домен: `curl http://your-domain.com`

## ✅ Настройка SSL

- [ ] Установка Certbot: `sudo apt install -y certbot python3-certbot-nginx`
- [ ] Получение сертификата: `sudo certbot --nginx -d your-domain.com`
- [ ] Проверка автоматического обновления: `sudo certbot renew --dry-run`
- [ ] Проверка HTTPS: `curl https://your-domain.com`

## ✅ Настройка домена в Selectel

- [ ] Вход в панель управления Selectel
- [ ] Переход в раздел "Домены"
- [ ] Добавление A-записи для основного домена:
  - Тип: A
  - Имя: @ (или пусто)
  - Значение: IP адрес вашего сервера
- [ ] Добавление A-записи для www поддомена:
  - Тип: A
  - Имя: www
  - Значение: IP адрес вашего сервера
- [ ] Ожидание распространения DNS (обычно 5-30 минут)

## ✅ Безопасность

- [ ] Настройка firewall:
  ```bash
  sudo ufw allow 22/tcp
  sudo ufw allow 80/tcp
  sudo ufw allow 443/tcp
  sudo ufw enable
  ```
- [ ] Проверка открытых портов: `sudo ufw status`
- [ ] Настройка автоматических обновлений (опционально)

## ✅ Финальная проверка

- [ ] Доступность приложения по HTTPS: `https://your-domain.com`
- [ ] Проверка входа в систему
- [ ] Проверка работы основных функций
- [ ] Проверка логов на ошибки: `docker-compose logs app`
- [ ] Настройка мониторинга (опционально)

## 📝 Полезные команды

```bash
# Просмотр логов
docker-compose logs -f app
docker-compose logs -f postgres

# Перезапуск контейнеров
docker-compose restart

# Остановка контейнеров
docker-compose down

# Обновление приложения
cd /opt/ai-crm/my-app
git pull
docker-compose up -d --build
docker-compose exec app npx prisma migrate deploy

# Резервное копирование БД
docker-compose exec postgres pg_dump -U crm_user crm_db > backup_$(date +%Y%m%d).sql
```

## 🆘 Устранение проблем

Если что-то пошло не так:

1. **Проверьте логи**: `docker-compose logs app`
2. **Проверьте статус контейнеров**: `docker-compose ps`
3. **Проверьте подключение к БД**: `docker-compose exec app npx prisma db pull`
4. **Проверьте Nginx логи**: `sudo tail -f /var/log/nginx/error.log`
5. **Проверьте переменные окружения**: `docker-compose exec app env | grep -E 'DATABASE_URL|NEXTAUTH'`


