# Резюме реализации системы оплаты подписки

## ✅ Что реализовано

### 1. База данных
- ✅ Обновлена Prisma schema:
  - Добавлен enum `PayerType` (INDIVIDUAL, LEGAL)
  - Добавлены поля в Invoice:
    - `invoiceNumber` (String, unique) - автоматическая нумерация
    - `paymentPeriodMonths` (Int, default 1) - период оплаты: 1, 3, 6, 12 месяцев
    - `companyId` (Int, nullable) - ID компании плательщика
    - `payerType` (PayerType, default INDIVIDUAL) - тип плательщика
- ✅ Создана миграция: `20250112000000_add_invoice_fields/migration.sql`
- ⚠️ **ВАЖНО: Миграцию нужно применить на сервере!**

### 2. Утилиты
- ✅ Создан файл `lib/invoice-utils.ts`:
  - `generateInvoiceNumber()` - генерация уникального номера счета (INV-YYYYMMDD-NNNN)
  - `calculatePaymentAmount()` - расчет суммы платежа
  - `calculatePeriodEnd()` - расчет даты окончания периода

### 3. API Endpoints

#### Обновленные:
- ✅ `POST /api/billing/payment` - поддержка периодов 1,3,6,12 месяцев
  - Принимает `paymentPeriodMonths: 1 | 3 | 6 | 12`
  - Автоматически определяет тип плательщика (физ/юр лицо)
  - Для юр лиц возвращает ошибку (нужно использовать /api/billing/invoice/generate)
  - Генерирует номер счета автоматически
  - Возвращает `invoiceNumber`, `amount`, `paymentPeriodMonths`

- ✅ `POST /api/billing/webhook` - обновлен для правильной обработки периода
  - Обновляет `currentPeriodEnd` на основе `paymentPeriodMonths`

#### Новые:
- ✅ `POST /api/billing/invoice/generate` - генерация счета для юридических лиц
  - Создает подписку со статусом TRIAL
  - Генерирует счет с автоматическим номером
  - Возвращает данные счета и URL для скачивания PDF

- ✅ `GET /api/billing/invoice/[id]/pdf` - скачивание PDF счета
  - Генерирует PDF с информацией о счете
  - Включает ID плательщика и номер счета для подтверждения оплаты

- ✅ `POST /api/billing/invoice/[id]/confirm` - подтверждение оплаты (ручное)
  - Доступ: только для owner
  - Активирует подписку после подтверждения
  - Обновляет период окончания

- ✅ `POST /api/admin/extend-subscription` - продление подписки админом
  - Доступ: только для owner
  - Принимает `companyId` и `paymentPeriodMonths`
  - Для физ лиц создает платеж в YooKassa
  - Для юр лиц генерирует PDF счет
  - Возвращает `paymentUrl` или `pdfUrl`

### 4. UI Компоненты

- ✅ Создан компонент `ExtendSubscriptionModal.tsx`:
  - Модальное окно для выбора периода продления (1,3,6,12 месяцев)
  - Интегрирован в страницу `/ops`

- ✅ Обновлена страница `/ops`:
  - Добавлена колонка "ID компании"
  - Добавлена колонка "Действия" с кнопкой "Продлить подписку"
  - Кнопка показывается только для первого пользователя каждой компании
  - Интегрировано модальное окно продления подписки

### 5. Документация
- ✅ Создан документ `PAYMENT_STRUCTURE.md` с подробным описанием структуры

## ⚠️ Что нужно сделать

### Критично:
1. **Применить миграцию на сервере:**
   ```bash
   ssh selectel "cd /opt/flamecrm && npx prisma migrate deploy"
   ```
   Или применить SQL вручную из файла:
   `prisma/migrations/20250112000000_add_invoice_fields/migration.sql`

2. **Обновить Prisma Client на сервере:**
   ```bash
   ssh selectel "cd /opt/flamecrm && npx prisma generate"
   ```

### Опционально (можно сделать позже):
1. Добавить выбор периода оплаты на странице тарифов (`/company`)
   - Сейчас используется фиксированный период (1 месяц)
   - Можно добавить модальное окно с выбором периода перед оплатой

2. Добавить страницу для просмотра всех счетов компании
   - Список счетов
   - Статусы оплаты
   - Скачивание PDF

3. Улучшить PDF счет:
   - Добавить реквизиты вашей компании
   - Улучшить форматирование
   - Добавить QR-код для оплаты

## 📋 Как использовать

### Для физических лиц:
1. Пользователь выбирает тариф
2. Вызывается `POST /api/billing/payment` с `paymentPeriodMonths: 1|3|6|12`
3. Создается платеж в YooKassa
4. Пользователь перенаправляется на оплату
5. После оплаты webhook активирует подписку

### Для юридических лиц:
1. Пользователь выбирает тариф
2. Вызывается `POST /api/billing/invoice/generate` с `paymentPeriodMonths: 1|3|6|12`
3. Генерируется счет с номером
4. Пользователь скачивает PDF счета
5. Админ (owner) подтверждает оплату через `POST /api/billing/invoice/[id]/confirm`

### Продление подписки (owner):
1. На странице `/ops` нажать "Продлить подписку"
2. Выбрать период (1,3,6,12 месяцев)
3. Для физ лиц - редирект на YooKassa
4. Для юр лиц - скачать PDF счета

## 🔍 Тестирование

После применения миграции:
1. Проверить создание счета для физ лиц
2. Проверить создание счета для юр лиц
3. Проверить продление подписки через `/ops`
4. Проверить генерацию PDF счета
5. Проверить подтверждение оплаты (для owner)
