# 🔐 Создание тестового аккаунта

## Способ 1: Через веб-интерфейс (рекомендуется)

1. Откройте https://flamecrm.ru/register
2. Заполните форму:
   - **Email:** `test@flamecrm.ru`
   - **Пароль:** `Test123456!`
   - **Имя:** `Тестовый пользователь`
3. Нажмите "Зарегистрироваться"
4. После регистрации войдите в систему

## Способ 2: Через SQL (если регистрация закрыта)

Выполните на сервере:

```bash
cd /opt/flamecrm
docker-compose exec -T postgres psql -U crm_user -d crm_db << EOF
INSERT INTO "User" (email, name, password, role, "companyId", "createdAt", "updatedAt")
SELECT 
  'test@flamecrm.ru',
  'Тестовый пользователь',
  password,
  'admin',
  "companyId",
  NOW(),
  NOW()
FROM "User"
WHERE email = 'info@flamecrm.ru'
ON CONFLICT (email) DO UPDATE
SET 
  name = EXCLUDED.name,
  role = EXCLUDED.role,
  "updatedAt" = NOW();
EOF
```

**Примечание:** Этот способ создаст пользователя с тем же паролем, что и у `info@flamecrm.ru`. После создания можно изменить пароль через интерфейс.

## Данные тестового аккаунта

После создания:
- **Email:** `test@flamecrm.ru`
- **Пароль:** `Test123456!` (или пароль от info@flamecrm.ru, если создан через SQL)
- **Роль:** `admin`
- **Ссылка для входа:** https://flamecrm.ru/login

## Изменение пароля

Если нужно изменить пароль:
1. Войдите в систему
2. Перейдите в настройки профиля
3. Измените пароль на `Test123456!`

