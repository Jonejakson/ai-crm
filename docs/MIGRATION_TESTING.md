# 🧪 Инструкция по тестированию миграции данных

## Обзор

Система позволяет импортировать данные из AmoCRM и Bitrix24 в вашу CRM. Есть два способа импорта:
1. **Через API** - прямое подключение к AmoCRM/Bitrix24 API
2. **Через файл** - загрузка экспортированных данных в формате JSON

---

## 📋 Подготовка к тестированию

### 1. Получение данных из AmoCRM

#### Вариант A: Экспорт через API (рекомендуется)

1. Войдите в ваш AmoCRM аккаунт
2. Перейдите в **Настройки → Интеграции → API**
3. Создайте новое приложение или используйте существующее
4. Получите **Access Token**
5. Запишите ваш **subdomain** (например, если URL `yourcompany.amocrm.ru`, то subdomain = `yourcompany`)

#### Вариант B: Экспорт вручную

1. В AmoCRM перейдите в **Настройки → Экспорт данных**
2. Экспортируйте:
   - Контакты
   - Сделки
   - Воронки продаж
3. Сохраните в формате JSON

**Формат JSON для AmoCRM:**
```json
{
  "contacts": [
    {
      "id": 123,
      "name": "Иван Иванов",
      "first_name": "Иван",
      "last_name": "Иванов",
      "custom_fields_values": [
        {
          "field_id": 1,
          "field_name": "Email",
          "values": [{"value": "ivan@example.com"}]
        },
        {
          "field_id": 2,
          "field_name": "Phone",
          "values": [{"value": "+79991234567"}]
        }
      ]
    }
  ],
  "deals": [
    {
      "id": 456,
      "name": "Продажа товара",
      "price": 100000,
      "status_id": 12345,
      "pipeline_id": 1,
      "contacts": [{"id": 123}]
    }
  ],
  "pipelines": [
    {
      "id": 1,
      "name": "Основная воронка",
      "statuses": [
        {"id": 12345, "name": "Первичный контакт", "color": "#3B82F6"},
        {"id": 12346, "name": "Коммерческое предложение", "color": "#10B981"}
      ]
    }
  ]
}
```

### 2. Получение данных из Bitrix24

#### Вариант A: Экспорт через API

1. Войдите в ваш Bitrix24
2. Перейдите в **Настройки → Приложения → Вебхуки**
3. Создайте вебхук с правами на чтение контактов и сделок
4. Получите **Access Token**
5. Запишите ваш **domain** (например, `yourcompany.bitrix24.ru`)

#### Вариант B: Экспорт вручную

1. В Bitrix24 используйте встроенный экспорт или API
2. Экспортируйте:
   - Контакты
   - Сделки
   - Этапы сделок
3. Сохраните в формате JSON

**Формат JSON для Bitrix24:**
```json
{
  "contacts": [
    {
      "ID": "123",
      "NAME": "Иван",
      "LAST_NAME": "Иванов",
      "EMAIL": [{"VALUE": "ivan@example.com", "VALUE_TYPE": "WORK"}],
      "PHONE": [{"VALUE": "+79991234567", "VALUE_TYPE": "WORK"}],
      "COMPANY_TITLE": "ООО Компания",
      "POST": "Менеджер"
    }
  ],
  "deals": [
    {
      "ID": "456",
      "TITLE": "Продажа товара",
      "OPPORTUNITY": "100000",
      "CURRENCY_ID": "RUB",
      "STAGE_ID": "NEW",
      "CONTACT_ID": "123"
    }
  ],
  "stages": [
    {
      "STATUS_ID": "NEW",
      "NAME": "Новая",
      "COLOR": "#3B82F6"
    }
  ]
}
```

---

## 🧪 Тестирование миграции

### Шаг 1: Доступ к разделу миграции

1. Войдите в CRM как администратор
2. Перейдите в **Компания** (в боковом меню)
3. Прокрутите вниз до раздела **"Миграция данных"**

### Шаг 2: Тестирование импорта из AmoCRM

#### Тест 1: Импорт через файл (проще всего)

1. **Выберите источник:** AmoCRM
2. **Загрузите файл:**
   - Нажмите "Или загрузите экспортированные данные (JSON)"
   - Выберите подготовленный JSON файл с данными AmoCRM
3. **Настройте параметры:**
   - Выберите **Пользователь по умолчанию** (обязательно)
   - Опционально: выберите воронку, источник сделок, тип сделок
4. **Нажмите "Начать импорт"**
5. **Проверьте результаты:**
   - Должно появиться сообщение "Импорт завершен"
   - В результатах указано количество созданных/пропущенных контактов и сделок

#### Тест 2: Импорт через API (требует настройки)

1. **Выберите источник:** AmoCRM
2. **Введите данные:**
   - Subdomain: ваш subdomain AmoCRM
   - Access Token: ваш токен доступа
3. **Настройте параметры** (как в Тесте 1)
4. **Нажмите "Начать импорт"**

**Примечание:** Для работы через API нужен активный доступ к AmoCRM API. Если токен недействителен, будет ошибка.

### Шаг 3: Тестирование импорта из Bitrix24

#### Тест 1: Импорт через файл

1. **Выберите источник:** Bitrix24
2. **Загрузите файл:**
   - Выберите подготовленный JSON файл с данными Bitrix24
3. **Настройте параметры** (как для AmoCRM)
4. **Нажмите "Начать импорт"**
5. **Проверьте результаты**

#### Тест 2: Импорт через API

1. **Выберите источник:** Bitrix24
2. **Введите данные:**
   - Domain: ваш domain Bitrix24
   - Access Token: ваш токен доступа
3. **Настройте параметры**
4. **Нажмите "Начать импорт"**

### Шаг 4: Проверка импортированных данных

После успешного импорта проверьте:

1. **Контакты:**
   - Перейдите в раздел **Контакты**
   - Убедитесь, что импортированные контакты присутствуют
   - Проверьте, что поля (имя, email, телефон, компания) заполнены корректно

2. **Сделки:**
   - Перейдите в раздел **Сделки**
   - Убедитесь, что импортированные сделки присутствуют
   - Проверьте привязку к контактам
   - Проверьте суммы и этапы

3. **Воронки (для AmoCRM):**
   - Перейдите в настройки воронок
   - Убедитесь, что воронки из AmoCRM созданы
   - Проверьте этапы воронок

### Шаг 5: Тестирование обработки дубликатов

1. **Повторите импорт** с теми же данными
2. **Проверьте результаты:**
   - Должно быть сообщение о пропущенных контактах/сделках
   - Дубликаты не должны создаваться повторно
   - Система определяет дубликаты по email или телефону

---

## 🐛 Тестирование ошибок

### Тест 1: Невалидный JSON файл

1. Загрузите файл с невалидным JSON
2. **Ожидаемый результат:** Ошибка "Ошибка чтения файла"

### Тест 2: Отсутствие обязательных полей

1. Попробуйте начать импорт без выбора пользователя
2. **Ожидаемый результат:** Ошибка "Выберите пользователя по умолчанию"

### Тест 3: Неверные учетные данные API

1. Введите неверный subdomain/domain или access token
2. **Ожидаемый результат:** Ошибка при попытке подключения к API

### Тест 4: Пустой файл

1. Загрузите пустой JSON файл `{}`
2. **Ожидаемый результат:** Импорт завершится, но ничего не создастся

---

## 📊 Примеры тестовых данных

### Минимальный набор для AmoCRM

```json
{
  "contacts": [
    {
      "id": 1,
      "name": "Тестовый Контакт",
      "custom_fields_values": [
        {
          "field_id": 1,
          "field_name": "Email",
          "values": [{"value": "test@example.com"}]
        }
      ]
    }
  ],
  "deals": [
    {
      "id": 1,
      "name": "Тестовая сделка",
      "price": 50000,
      "contacts": [{"id": 1}]
    }
  ],
  "pipelines": []
}
```

### Минимальный набор для Bitrix24

```json
{
  "contacts": [
    {
      "ID": "1",
      "NAME": "Тестовый",
      "LAST_NAME": "Контакт",
      "EMAIL": [{"VALUE": "test@example.com", "VALUE_TYPE": "WORK"}]
    }
  ],
  "deals": [
    {
      "ID": "1",
      "TITLE": "Тестовая сделка",
      "OPPORTUNITY": "50000",
      "CONTACT_ID": "1"
    }
  ],
  "stages": []
}
```

---

## ✅ Чеклист тестирования

- [ ] Импорт контактов из AmoCRM (через файл)
- [ ] Импорт сделок из AmoCRM (через файл)
- [ ] Импорт воронок из AmoCRM (через файл)
- [ ] Импорт контактов из Bitrix24 (через файл)
- [ ] Импорт сделок из Bitrix24 (через файл)
- [ ] Импорт через API AmoCRM (если есть доступ)
- [ ] Импорт через API Bitrix24 (если есть доступ)
- [ ] Проверка обработки дубликатов
- [ ] Проверка корректности данных после импорта
- [ ] Тестирование ошибок (невалидный JSON, отсутствие полей)
- [ ] Проверка привязки сделок к контактам
- [ ] Проверка назначения пользователя по умолчанию

---

## 🔧 Устранение проблем

### Проблема: "Ошибка чтения файла"

**Решение:**
- Убедитесь, что файл в формате JSON
- Проверьте синтаксис JSON (можно использовать валидатор)
- Убедитесь, что файл не пустой

### Проблема: "Пользователь не найден"

**Решение:**
- Убедитесь, что выбран существующий пользователь
- Проверьте, что вы вошли как администратор

### Проблема: "Контакт не найден" при импорте сделок

**Решение:**
- Убедитесь, что контакты импортированы перед сделками
- Проверьте, что в данных сделок указаны правильные ID контактов

### Проблема: Данные не импортируются

**Решение:**
- Проверьте консоль браузера на наличие ошибок
- Проверьте логи сервера
- Убедитесь, что формат данных соответствует ожидаемому

---

## 📝 Примечания

1. **Производительность:** При импорте большого количества данных (1000+ записей) процесс может занять время. Дождитесь завершения.

2. **Безопасность:** Access Token для API хранится только в сессии и не сохраняется на сервере.

3. **Дубликаты:** Система автоматически определяет дубликаты по email или телефону. Если контакт уже существует, он будет пропущен.

4. **Маппинг полей:** В будущих версиях будет добавлен визуальный маппинг полей для более гибкой настройки импорта.

---

**Дата создания:** 2024-12-XX  
**Версия:** 1.0

