# 📊 Настройка мониторинга

## Обзор

В проекте настроен комплексный мониторинг с использованием:
- **Sentry** - отслеживание ошибок и производительности (опционально)
- **Overseer** - российская альтернатива для мониторинга доступности (рекомендуется) 🇷🇺
- **Структурированное логирование** - централизованное логирование
- **Health check** - проверка состояния приложения

> 💡 **Для российских пользователей:** Рекомендуем использовать **Overseer** вместо Sentry для мониторинга доступности. См. `MONITORING_OVERSEER.md`

---

## 1. Настройка Sentry

> **💡 Важно:** Sentry имеет **бесплатный план** (Developer Plan):
> - 5,000 ошибок в месяц
> - 10,000 performance units в месяц
> - 1 проект
> 
> Для начала этого достаточно! Если нужны альтернативы, см. `MONITORING_ALTERNATIVES.md`

### Шаг 1: Создание проекта в Sentry

1. Зарегистрируйтесь на [sentry.io](https://sentry.io) (бесплатно)
2. Создайте новый проект:
   - Выберите платформу: **Next.js**
   - Название проекта: `pocket-crm` (или любое другое)

3. Получите DSN (Data Source Name):
   - После создания проекта скопируйте DSN
   - Формат: `https://xxxxx@sentry.io/xxxxx`

### Шаг 2: Настройка переменных окружения

Добавьте в `.env`:

```env
# Для клиентской части (браузер)
NEXT_PUBLIC_SENTRY_DSN=https://your-dsn@sentry.io/project-id

# Для серверной части (Node.js)
SENTRY_DSN=https://your-dsn@sentry.io/project-id

# Для автоматической загрузки source maps (опционально)
SENTRY_ORG=your-org-name
SENTRY_PROJECT=your-project-name
SENTRY_AUTH_TOKEN=your-auth-token
```

**Где получить:**
- `SENTRY_ORG` - название вашей организации в Sentry
- `SENTRY_PROJECT` - название проекта
- `SENTRY_AUTH_TOKEN` - создайте на [Settings → Auth Tokens](https://sentry.io/settings/account/api/auth-tokens/)

### Шаг 3: Проверка работы

1. Запустите приложение:
   ```bash
   npm run dev
   ```

2. Создайте тестовую ошибку (например, откройте несуществующую страницу)

3. Проверьте в Sentry Dashboard, что ошибка появилась

---

## 2. Использование логгера

### Базовое использование

```typescript
import { logger } from "@/lib/logger";

// Информационное сообщение
logger.info("Пользователь вошел в систему", { userId: 123 });

// Предупреждение
logger.warn("Превышен лимит запросов", { userId: 123, limit: 100 });

// Ошибка
logger.error("Ошибка при создании сделки", error, { dealId: 456 });

// Критическая ошибка
logger.fatal("Критическая ошибка БД", error, { query: "SELECT * FROM deals" });
```

### Логирование с контекстом

```typescript
logger.info("Сделка создана", {
  dealId: 123,
  amount: 50000,
  stage: "negotiation",
}, {
  userId: 456,
  companyId: 789,
  requestId: "req-123",
});
```

### Логирование API запросов

```typescript
// В API endpoint
logger.logRequest("POST", "/api/deals", 201, 150, {
  userId: 123,
  companyId: 456,
});
```

---

## 3. Health Check Endpoint

### Использование

Health check доступен по адресу: `/api/health`

**Пример запроса:**
```bash
curl http://localhost:3000/api/health
```

**Пример ответа:**
```json
{
  "status": "healthy",
  "timestamp": "2025-12-01T12:00:00.000Z",
  "uptime": 3600,
  "checks": {
    "database": {
      "status": "ok",
      "responseTime": 15
    },
    "encryption": {
      "status": "ok"
    }
  },
  "version": "0.1.0",
  "environment": "production"
}
```

**Статусы:**
- `healthy` - все проверки прошли успешно
- `degraded` - есть проблемы, но приложение работает
- `unhealthy` - критические проблемы

**HTTP коды:**
- `200` - healthy или degraded
- `503` - unhealthy

### Настройка мониторинга

Настройте внешний мониторинг (например, UptimeRobot, Pingdom) для проверки `/api/health` каждые 1-5 минут.

---

## 4. Интеграция в API endpoints

### Использование обертки для API handlers

```typescript
import { withApiHandler, createSuccessResponse } from "@/lib/api-wrapper";
import { logger } from "@/lib/logger";

export const GET = withApiHandler(async (req) => {
  const user = await getCurrentUser();
  
  // Логирование с контекстом
  logger.info("Получение контактов", { userId: user.id });
  
  const contacts = await prisma.contact.findMany();
  
  return createSuccessResponse(contacts, {
    userId: user.id,
    companyId: user.companyId,
  });
});
```

### Обработка ошибок

```typescript
import { createErrorResponse } from "@/lib/api-wrapper";

try {
  // ваш код
} catch (error) {
  return createErrorResponse("Ошибка при создании контакта", 500, error);
}
```

---

## 5. Настройка алертов в Sentry

### Email уведомления

1. Перейдите в **Settings → Projects → [Ваш проект] → Alerts**
2. Создайте правило алерта:
   - **Trigger**: Когда количество ошибок превышает порог
   - **Conditions**: Более 10 ошибок за 1 минуту
   - **Actions**: Отправить email

### Slack интеграция

1. Перейдите в **Settings → Integrations → Slack**
2. Подключите Slack workspace
3. Настройте канал для уведомлений
4. Создайте правило алерта с отправкой в Slack

---

## 6. Мониторинг производительности

### Traces Sample Rate

В `sentry.client.config.ts` и `sentry.server.config.ts` настроен `tracesSampleRate`:
- **Development**: 1.0 (100% запросов)
- **Production**: 0.1 (10% запросов)

Это позволяет отслеживать производительность без перегрузки квоты Sentry.

### Просмотр метрик

1. Перейдите в Sentry Dashboard
2. Откройте **Performance**
3. Просмотрите:
   - Время ответа API
   - Медленные запросы
   - Топ транзакций

---

## 7. Best Practices

### Что логировать

✅ **Логируйте:**
- Ошибки и исключения
- Важные действия (создание сделок, платежи)
- Медленные запросы (>1 секунды)
- Изменения прав доступа
- Критические операции

❌ **Не логируйте:**
- Пароли и токены (даже в зашифрованном виде)
- Персональные данные (GDPR)
- Избыточная информация

### Уровни логирования

- **DEBUG** - детальная информация (только development)
- **INFO** - информационные сообщения
- **WARN** - предупреждения
- **ERROR** - ошибки (отправляются в Sentry)
- **FATAL** - критические ошибки (отправляются в Sentry)

### Контекст в логах

Всегда добавляйте контекст:
```typescript
logger.error("Ошибка", error, {
  userId: user.id,
  companyId: user.companyId,
  action: "create_deal",
  dealId: deal.id,
});
```

---

## 8. Troubleshooting

### Sentry не отправляет ошибки

1. Проверьте, что `SENTRY_DSN` установлен в `.env`
2. Проверьте, что `NODE_ENV=production` или DSN явно указан
3. Проверьте консоль браузера на ошибки подключения к Sentry

### Логи не появляются

1. Проверьте `NODE_ENV` - в production логируются только WARN и выше
2. Убедитесь, что используете правильный уровень логирования

### Health check возвращает unhealthy

1. Проверьте подключение к БД (`DATABASE_URL`)
2. Проверьте, что `ENCRYPTION_KEY` установлен и имеет минимум 32 символа
3. Проверьте логи приложения

---

## 9. Дополнительные ресурсы

- [Sentry Next.js документация](https://docs.sentry.io/platforms/javascript/guides/nextjs/)
- [Sentry Dashboard](https://sentry.io)
- [Структурированное логирование](https://www.loggly.com/ultimate-guide/node-logging-basics/)

---

**Готово!** Мониторинг настроен и готов к использованию. 🚀

