# Развертывание восстановления пароля - ИТОГОВЫЙ ОТЧЕТ

## ✅ Что реализовано и развернуто

### 1. База данных
- ✅ Добавлены поля `passwordResetToken` и `passwordResetExpires` в модель `User`
- ✅ Миграция применена на сервере
- ✅ Колонки созданы в базе данных

### 2. API Endpoints
- ✅ `/api/auth/forgot-password` - запрос восстановления пароля
- ✅ `/api/auth/reset-password` - сброс пароля по токену
- ✅ Оба endpoint работают и доступны

### 3. Страницы
- ✅ `/forgot-password` - форма запроса восстановления
- ✅ `/reset-password?token=...` - форма сброса пароля
- ✅ Ссылка "Забыли пароль?" на странице логина
- ✅ Все страницы доступны без авторизации

### 4. Настройки SMTP
- ✅ Настроен SMTP для mail.ru:
  - Host: `smtp.mail.ru`
  - Port: `465` (SSL)
  - User: `info@flamecrm.ru`
  - Password: `oLn05BfskIxWZSE2zcWi`
  - From: `info@flamecrm.ru`
- ✅ `NEXTAUTH_URL` настроен: `https://flamecrm.ru`

### 5. Безопасность
- ✅ Токен действителен 24 часа
- ✅ Токен одноразовый (удаляется после использования)
- ✅ Защита от перебора email'ов
- ✅ Валидация пароля (минимум 6 символов)

## 📋 Текущие настройки на сервере

Файл `.env` на сервере содержит:
```env
MAIL_HOST=smtp.mail.ru
MAIL_PORT=465
MAIL_USER=info@flamecrm.ru
MAIL_PASSWORD=oLn05BfskIxWZSE2zcWi
MAIL_FROM=info@flamecrm.ru
NEXTAUTH_URL=https://flamecrm.ru
```

## 🧪 Как проверить работу

1. Откройте `https://flamecrm.ru/login`
2. Нажмите "Забыли пароль?"
3. Введите email пользователя (например, `sale2@barier-yug.ru`)
4. Проверьте почту `info@flamecrm.ru` (включая папку "Спам")
5. Перейдите по ссылке из письма
6. Введите новый пароль (минимум 6 символов)
7. Автоматически перенаправит на страницу входа

## ⚠️ Важные замечания

### Пароль для mail.ru
Пароль `oLn05BfskIxWZSE2zcWi` - это **пароль приложения**, а не основной пароль от почты.

**Где взять пароль приложения для mail.ru:**
1. Зайдите на https://mail.ru
2. Перейдите в Настройки → Безопасность
3. Включите "Пароли приложений" (если не включено)
4. Создайте новый пароль приложения для "Почтовый клиент"
5. Скопируйте пароль и обновите `MAIL_PASSWORD` в `.env`

### Если письма не приходят

1. **Проверьте папку "Спам"** - письма могут попадать туда
2. **Проверьте логи:**
   ```bash
   docker-compose logs app | grep -i email
   ```
3. **Проверьте настройки SMTP** - возможно, нужно использовать другой порт (587 для TLS)
4. **В режиме разработки** токен возвращается в ответе API (если SMTP не настроен)

## 🔧 Альтернативные SMTP провайдеры

Если mail.ru не работает, можно использовать:

### Gmail
```env
MAIL_HOST=smtp.gmail.com
MAIL_PORT=465
MAIL_USER=your-email@gmail.com
MAIL_PASSWORD=app-password  # Пароль приложения, не основной!
MAIL_FROM=your-email@gmail.com
```

**Как получить пароль приложения Gmail:**
1. Включите 2FA в аккаунте Google
2. Перейдите в Настройки аккаунта → Безопасность
3. Создайте "Пароль приложения"
4. Используйте его как `MAIL_PASSWORD`

### Yandex
```env
MAIL_HOST=smtp.yandex.ru
MAIL_PORT=465
MAIL_USER=your-email@yandex.ru
MAIL_PASSWORD=app-password
MAIL_FROM=your-email@yandex.ru
```

## 📝 Статус развертывания

- ✅ Код обновлен на сервере
- ✅ Миграция применена
- ✅ Контейнер пересобран
- ✅ Приложение работает
- ✅ SMTP настроен
- ✅ Страницы доступны

**Восстановление пароля полностью готово к использованию!**

