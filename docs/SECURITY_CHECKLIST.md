# 🔒 Чеклист безопасности

## ✅ Что уже реализовано

### 1. Security Headers (middleware.ts)
- ✅ `X-Frame-Options: DENY` - защита от clickjacking
- ✅ `X-Content-Type-Options: nosniff` - защита от MIME sniffing
- ✅ `X-XSS-Protection: 1; mode=block` - защита от XSS
- ✅ `Referrer-Policy: strict-origin-when-cross-origin` - контроль referrer
- ✅ `Permissions-Policy` - ограничение доступа к API браузера
- ✅ `Content-Security-Policy` - защита от XSS и инъекций
- ✅ `Strict-Transport-Security` - принудительный HTTPS (для продакшена)

### 2. Rate Limiting
- ✅ Защита от DDoS (`lib/rate-limit.ts`)
- ✅ Разные лимиты для разных типов endpoints
- ✅ Блокировка при превышении лимитов

### 3. Аутентификация и авторизация
- ✅ NextAuth.js для аутентификации
- ✅ Проверка сессий в middleware
- ✅ Роли пользователей (admin, manager, user)
- ✅ Фильтрация данных по компаниям (`lib/access-control.ts`)

### 4. Валидация данных
- ✅ Zod схемы валидации (`lib/validation/`)
- ✅ Валидация на сервере
- ✅ Защита от SQL-инъекций (Prisma ORM)

### 5. Шифрование
- ✅ Шифрование чувствительных данных (`lib/encryption.ts`)
- ✅ Шифрование паролей email интеграций
- ✅ Шифрование OAuth токенов
- ✅ Шифрование API ключей

### 6. Webhook Security
- ✅ Валидация подписей webhook'ов (`lib/webhook-security.ts`)
- ✅ HMAC-SHA256 для YooKassa
- ✅ Проверка подписей для всех входящих webhook'ов

---

## ⚠️ Что нужно проверить

### 1. CORS настройки

**Статус:** Next.js по умолчанию не требует CORS для same-origin запросов.

**Проверить:**
- [ ] Если нужны cross-origin запросы, добавить CORS headers в API routes
- [ ] Убедиться, что разрешены только нужные домены
- [ ] Проверить preflight запросы (OPTIONS)

**Если нужен CORS:**
```typescript
// app/api/example/route.ts
import { NextResponse } from 'next/server'

export async function GET() {
  const response = NextResponse.json({ data: 'example' })
  
  // CORS headers (если нужны)
  response.headers.set('Access-Control-Allow-Origin', 'https://yourdomain.com')
  response.headers.set('Access-Control-Allow-Methods', 'GET, POST, OPTIONS')
  response.headers.set('Access-Control-Allow-Headers', 'Content-Type, Authorization')
  
  return response
}
```

### 2. CSRF защита

**Статус:** Next.js имеет встроенную защиту от CSRF.

**Проверить:**
- [ ] Все формы используют Next.js формы или API routes
- [ ] Нет прямых POST запросов к внешним API из форм
- [ ] API routes проверяют origin (если нужно)

**Next.js автоматически:**
- Проверяет origin для POST/PUT/DELETE запросов
- Блокирует запросы с неправильным origin
- Защищает от CSRF атак

### 3. Валидация входных данных

**Статус:** Частично реализовано.

**Проверить:**
- [ ] Все API endpoints используют валидацию Zod
- [ ] Валидация на клиенте (react-hook-form + zod)
- [ ] Санитизация пользовательского ввода (защита от XSS)

**Пример использования:**
```typescript
import { validateRequest, createContactSchema } from "@/lib/validation";

export async function POST(req: Request) {
  const body = await req.json();
  const validation = validateRequest(createContactSchema, body);
  
  if (validation instanceof NextResponse) {
    return validation; // Ошибка валидации
  }
  
  // Используем валидированные данные
  const data = validation;
}
```

### 4. Защита от SQL-инъекций

**Статус:** ✅ Защищено (Prisma ORM).

**Prisma автоматически:**
- Параметризует все запросы
- Экранирует специальные символы
- Защищает от SQL-инъекций

**Проверить:**
- [ ] Нет использования `$queryRaw` с пользовательским вводом без параметризации
- [ ] Все запросы используют Prisma методы или параметризованные `$queryRaw`

### 5. Защита от XSS

**Статус:** Частично реализовано.

**Проверить:**
- [ ] Санитизация пользовательского ввода перед отображением
- [ ] Использование `dangerouslySetInnerHTML` только с санитизацией
- [ ] Content Security Policy настроен правильно

**Рекомендации:**
- Использовать библиотеку для санитизации (например, DOMPurify)
- Избегать `dangerouslySetInnerHTML` где возможно
- Экранировать HTML в пользовательском контенте

### 6. Защита паролей

**Статус:** ✅ Защищено (bcryptjs).

**Проверить:**
- [ ] Все пароли хешируются с bcrypt
- [ ] Минимальная длина пароля (рекомендуется 8+ символов)
- [ ] Проверка сложности пароля (опционально)

### 7. Защита API ключей

**Статус:** ✅ Защищено (шифрование).

**Проверить:**
- [ ] Все API ключи шифруются перед сохранением
- [ ] `ENCRYPTION_KEY` установлен в production
- [ ] Ключи не логируются

### 8. Логирование чувствительных данных

**Проверить:**
- [ ] Пароли не логируются
- [ ] Токены не логируются
- [ ] API ключи не логируются
- [ ] Персональные данные не логируются (GDPR)

---

## 🔍 Аудит безопасности

### Проверка API endpoints

Проверить все API endpoints на:
- [ ] Авторизацию (требуется ли вход)
- [ ] Валидацию входных данных
- [ ] Проверку прав доступа (роли)
- [ ] Фильтрацию данных по компаниям (мультитенантность)
- [ ] Обработку ошибок (не раскрывать внутренние детали)

### Проверка форм

Проверить все формы на:
- [ ] Валидацию на клиенте
- [ ] Валидацию на сервере
- [ ] Защиту от CSRF
- [ ] Ограничение размера загружаемых файлов

### Проверка файлов

Проверить загрузку файлов на:
- [ ] Валидацию типов файлов (MIME types)
- [ ] Ограничение размера файлов
- [ ] Сканирование на вирусы (опционально)
- [ ] Безопасное хранение файлов

---

## 🛡️ Рекомендации

### 1. Регулярные обновления
- [ ] Регулярно обновлять зависимости: `npm audit fix`
- [ ] Проверять уязвимости: `npm audit`
- [ ] Обновлять Node.js до последней LTS версии

### 2. Мониторинг
- [ ] Настроить мониторинг ошибок (Sentry или Overseer)
- [ ] Логировать подозрительную активность
- [ ] Настроить алерты на аномалии

### 3. Резервное копирование
- [ ] Регулярные бэкапы базы данных
- [ ] Хранение бэкапов в безопасном месте
- [ ] Тестирование восстановления из бэкапа

### 4. SSL/TLS
- [ ] Использовать HTTPS в production
- [ ] Валидный SSL сертификат
- [ ] HSTS заголовок (уже настроен в middleware)

---

## 📋 Быстрая проверка перед релизом

- [ ] Все security headers настроены
- [ ] Rate limiting работает
- [ ] Валидация данных на всех endpoints
- [ ] Шифрование чувствительных данных
- [ ] Webhook подписи проверяются
- [ ] Нет чувствительных данных в логах
- [ ] HTTPS настроен
- [ ] Зависимости обновлены (`npm audit`)
- [ ] Тесты безопасности пройдены

---

**Дата проверки:** _______________
**Проверил:** _______________

