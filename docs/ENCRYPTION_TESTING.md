# 🧪 Тестирование шифрования данных

## Быстрая проверка

### 1. Настройка ключа шифрования

1. Сгенерируйте ключ:
   ```bash
   openssl rand -hex 32
   ```

2. Добавьте в `.env`:
   ```env
   ENCRYPTION_KEY=ваш_ключ_64_символа
   ```

3. Перезапустите приложение

### 2. Тестирование шифрования Email интеграций

#### Тест 1: Создание IMAP/SMTP интеграции

1. Перейдите в **Компания → Email интеграции**
2. Создайте новую интеграцию с типом **IMAP/SMTP**
3. Введите пароль
4. Сохраните

**Проверка:**
- Откройте базу данных (Prisma Studio: `npm run db:studio`)
- Найдите таблицу `EmailIntegration`
- Проверьте поле `imapPassword` или `smtpPassword`
- Должно быть зашифровано (формат: `iv:salt:tag:encryptedData`)

#### Тест 2: Создание Gmail/Outlook интеграции

1. Создайте интеграцию с типом **Gmail** или **Outlook**
2. Введите OAuth токены
3. Сохраните

**Проверка:**
- В базе данных проверьте поля `accessToken` и `refreshToken`
- Должны быть зашифрованы

#### Тест 3: Использование зашифрованных данных

1. Создайте email интеграцию с паролем
2. Попробуйте синхронизировать письма (кнопка "Синхронизировать")
3. Попробуйте отправить письмо

**Ожидаемый результат:**
- Синхронизация и отправка должны работать
- Пароль автоматически расшифровывается при использовании

### 3. Тестирование шифрования мессенджеров

#### Тест 1: Telegram Bot

1. Перейдите в **Компания → Telegram Bot**
2. Введите Bot Token
3. Сохраните

**Проверка:**
- В базе данных (`MessagingIntegration`) поле `botToken` должно быть зашифровано
- Попробуйте установить webhook - должно работать

#### Тест 2: WhatsApp

1. Перейдите в **Компания → WhatsApp**
2. Введите API Key и Webhook Verify Token
3. Сохраните

**Проверка:**
- В базе данных поля `botToken` и `webhookSecret` должны быть зашифрованы

### 4. Тестирование шифрования систем учета

#### Тест 1: МойСклад

1. Перейдите в **Компания → МойСклад**
2. Введите логин и пароль
3. Сохраните

**Проверка:**
- В базе данных (`AccountingIntegration`) поле `apiSecret` должно быть зашифровано
- Попробуйте выгрузить сделку в МойСклад - должно работать

#### Тест 2: 1С

1. Перейдите в **Компания → 1С**
2. Введите URL, токен или логин/пароль
3. Сохраните

**Проверка:**
- В базе данных поля `apiToken` и `apiSecret` должны быть зашифрованы

### 5. Тестирование шифрования рекламных платформ

#### Тест 1: Яндекс.Директ

1. Перейдите в **Компания → Рекламные интеграции**
2. Настройте Яндекс.Директ
3. Введите OAuth токен и webhook secret
4. Сохраните

**Проверка:**
- В базе данных поля `apiToken` и `webhookSecret` должны быть зашифрованы

#### Тест 2: Авито

1. Настройте Авито интеграцию
2. Введите Client ID, Client Secret, webhook secret
3. Сохраните

**Проверка:**
- В базе данных поля `apiToken`, `apiSecret`, `webhookSecret` должны быть зашифрованы

### 6. Тестирование проверки HMAC подписей

#### Тест 1: YooKassa Webhook

1. Убедитесь, что `YOOKASSA_SECRET_KEY` установлен в `.env`
2. В production режиме webhook'и YooKassa будут проверяться автоматически
3. Неверные подписи будут отклоняться

**Проверка:**
- Отправьте тестовый webhook с неверной подписью
- Должна вернуться ошибка 401

#### Тест 2: Webhook'и рекламных платформ

1. Настройте webhook secret для Яндекс.Директ или Авито
2. Отправьте тестовый запрос с неверной подписью
3. Должна вернуться ошибка 403

## Проверка обратной совместимости

### Тест: Старые незашифрованные данные

1. Временно удалите `ENCRYPTION_KEY` из `.env`
2. Создайте интеграцию (данные сохранятся незашифрованными)
3. Добавьте `ENCRYPTION_KEY` обратно
4. Попробуйте использовать интеграцию

**Ожидаемый результат:**
- Старые незашифрованные данные должны работать
- При следующем обновлении интеграции данные будут зашифрованы

## Чеклист тестирования

- [ ] Email интеграции: пароли шифруются
- [ ] Email интеграции: OAuth токены шифруются
- [ ] Email интеграции: зашифрованные данные используются корректно
- [ ] Telegram Bot: botToken шифруется
- [ ] Telegram Bot: зашифрованный токен работает
- [ ] WhatsApp: API key и webhook secret шифруются
- [ ] WhatsApp: зашифрованные данные работают
- [ ] МойСклад: пароль шифруется
- [ ] МойСклад: зашифрованный пароль работает
- [ ] 1С: токены/пароли шифруются
- [ ] 1С: зашифрованные данные работают
- [ ] Яндекс.Директ: токены шифруются
- [ ] Яндекс.Директ: проверка webhook подписи работает
- [ ] Авито: токены шифруются
- [ ] Авито: проверка webhook подписи работает
- [ ] YooKassa: проверка HMAC подписи работает (production)
- [ ] Обратная совместимость: старые данные работают

## Устранение проблем

### Проблема: "Ошибка расшифровки данных"

**Решение:**
- Проверьте правильность `ENCRYPTION_KEY`
- Убедитесь, что ключ не был изменен после создания данных
- Проверьте логи на наличие ошибок

### Проблема: Интеграция не работает после добавления шифрования

**Решение:**
- Убедитесь, что `ENCRYPTION_KEY` установлен
- Пересоздайте интеграцию (введите данные заново)
- Проверьте, что данные корректно расшифровываются

### Проблема: Webhook подписи не проверяются

**Решение:**
- Убедитесь, что `YOOKASSA_SECRET_KEY` установлен (для YooKassa)
- Проверьте, что webhook secret настроен в интеграции
- В development режиме проверка может быть отключена

---

**Дата создания:** 2024-12-XX  
**Версия:** 1.0

