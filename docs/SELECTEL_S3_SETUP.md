# Настройка Selectel Object Storage для Flame CRM

## 📋 Что нужно сделать

### 1. Создать бакет в Selectel Object Storage

1. Войдите в панель управления Selectel: https://panel.selectel.com/
2. Перейдите в раздел **Облачное хранилище** → **Object Storage**
3. Создайте новый бакет (контейнер):
   - **Имя бакета:** `flamecrm-files` (или любое другое имя)
   - **Регион:** `ru-1` (Москва) или другой по вашему выбору
   - **Тип доступа:** Приватный (для безопасности)

### 2. Создать ключи доступа (Access Keys)

1. В панели Selectel перейдите в **Профиль** → **API ключи**
2. Создайте новый ключ для Object Storage:
   - **Тип:** Object Storage
   - **Имя:** `flamecrm-s3-key` (или любое другое)
3. Сохраните:
   - **Access Key ID** (публичный ключ)
   - **Secret Access Key** (секретный ключ) - показывается только один раз!

### 3. Настроить переменные окружения

Добавьте в файл `.env` на сервере:

```env
# Selectel Object Storage (S3)
S3_ACCESS_KEY_ID=ваш_access_key_id
S3_SECRET_ACCESS_KEY=ваш_secret_access_key
S3_BUCKET_NAME=flamecrm-files
S3_ENDPOINT=https://s3.selcdn.ru
S3_REGION=ru-1
S3_PUBLIC_URL=https://ваш-бакет.selcdn.ru
```

**Важно:**
- `S3_PUBLIC_URL` - опционально, если хотите публичный доступ к файлам
- Если не указать `S3_PUBLIC_URL`, будет использоваться endpoint с signed URLs
- `S3_ENDPOINT` по умолчанию `https://s3.selcdn.ru` (для Selectel)

### 4. Перезапустить приложение

```bash
cd /opt/flamecrm
docker-compose down
docker-compose up -d
```

### 5. Проверить работу

1. Загрузите тестовый файл через интерфейс CRM
2. Проверьте, что файл появился в бакете Selectel
3. Проверьте, что файл можно скачать через интерфейс

---

## 🔧 Структура хранения файлов

Файлы в S3 хранятся по следующей структуре:
```
{entityType}/{entityId}/{filename}
```

Например:
- `contact/123/1234567890_abc123.pdf`
- `deal/456/1234567891_def456.jpg`
- `task/789/1234567892_ghi789.png`

---

## 🔒 Безопасность

1. **Приватный бакет** - рекомендуется использовать приватный бакет
2. **Access Keys** - храните секретные ключи в безопасности
3. **Не коммитьте `.env`** - никогда не добавляйте `.env` в git
4. **Ротация ключей** - периодически обновляйте ключи доступа

---

## 📊 Мониторинг использования

В панели Selectel можно отслеживать:
- Объем хранилища
- Количество запросов
- Стоимость использования

---

## 🆘 Решение проблем

### Файлы не загружаются

1. Проверьте переменные окружения:
   ```bash
   docker-compose exec app env | grep S3_
   ```

2. Проверьте логи:
   ```bash
   docker-compose logs app | grep -i s3
   ```

3. Проверьте права доступа ключей в панели Selectel

### Ошибка "S3 credentials not configured"

- Убедитесь, что все переменные `S3_*` установлены в `.env`
- Перезапустите контейнер после изменения `.env`

### Файлы загружаются, но не скачиваются

- Проверьте, что ключ доступа имеет права на чтение
- Проверьте структуру ключа в S3 (должен быть `{entityType}/{entityId}/{filename}`)

---

## 💰 Стоимость

Selectel Object Storage:
- **Хранение:** от 1.5 ₽/ГБ/месяц
- **Запросы:** от 0.5 ₽ за 1000 запросов PUT/POST
- **Запросы GET:** от 0.3 ₽ за 1000 запросов

Подробнее: https://selectel.ru/services/cloud/storage/

---

## 📝 Дополнительная информация

- [Документация Selectel Object Storage](https://docs.selectel.com/storage/s3/)
- [S3 API совместимость](https://docs.selectel.com/storage/s3/api/)

