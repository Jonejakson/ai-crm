# 🔒 Отчёт аудита безопасности Flame CRM

**Дата:** 25 января 2026  
**Цель:** Проверка безопасности для платного продукта

---

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. Эндпоинт экстренного сброса пароля — слабый ключ по умолчанию

**Файл:** `app/api/admin/reset-password-emergency/route.ts`

**Проблема:** Если `EMERGENCY_PASSWORD_RESET_KEY` не задан в `.env`, используется дефолтный ключ `'emergency-reset-2024'`. Любой, кто знает этот ключ, может сбросить пароль **любого пользователя** по email.

**Риск:** Полный перехват аккаунтов.

**Рекомендация:**
- В production выбрасывать ошибку, если `EMERGENCY_PASSWORD_RESET_KEY` не установлен
- Никогда не использовать дефолтное значение для production
- Рассмотреть удаление этого эндпоинта или отключение при `NODE_ENV=production`

---

### 2. Нет валидации типов файлов при загрузке

**Файл:** `app/api/files/upload/route.ts`

**Проблема:** Принимаются любые файлы. Нет проверки MIME-типа и расширения. Можно загрузить `.exe`, `.php`, `.svg` с XSS и т.п.

**Риск:** 
- Загрузка вредоносного ПО
- XSS через SVG
- Исполняемые файлы на сервере (если есть неправильная конфигурация)

**Рекомендация:**
- Ввести whitelist разрешённых MIME-типов (например: `image/*`, `application/pdf`, `application/msword`, и т.д.)
- Проверять `file.type` и расширение
- Блокировать: `.exe`, `.php`, `.phtml`, `.svg` (или санитизировать SVG)

---

### 3. XSS в шаблонах писем

**Файл:** `app/email-templates/page.tsx` (строка ~486)

**Проблема:** `dangerouslySetInnerHTML` используется с HTML из `replaceTemplateVariables(previewTemplate.body, previewContext)`. Контент шаблона задаётся пользователем — без санитизации возможен XSS.

**Риск:** Кража сессий, перехват ввода, phishing.

**Рекомендация:**
- Подключить DOMPurify и санитизировать HTML перед выводом
- Либо выводить только текст (например, через `textContent`)

---

## ⚠️ ВАЖНЫЕ (не критичные)

### 4. Health endpoint раскрывает информацию

**Файл:** `app/api/health/route.ts`

**Проблема:** Возвращает: статус `ENCRYPTION_KEY` (есть/нет, длина), `environment`, `memoryUsage`. Endpoint публичный.

**Риск:** Упрощает разведку для атакующего.

**Рекомендация:**
- Ограничить доступ по IP (например, только мониторинг)
- Либо убрать детали про encryption из публичного ответа

---

### 5. Раздел `/api/billing/plans` без авторизации

**Файл:** `app/api/billing/plans/route.ts`

**Проблема:** Список тарифов доступен без авторизации.

**Риск:** Обычно допустимо для публичной страницы цен. Проверить, что не отдаются лишние данные.

---

### 6. Длина пароля при сбросе

**Файл:** `app/api/auth/reset-password/route.ts`

**Проблема:** Минимальная длина пароля — 6 символов.

**Рекомендация:** Увеличить до 8+ символов и добавить проверку сложности.

---

### 7. `layout.tsx` — `dangerouslySetInnerHTML`

**Файл:** `app/layout.tsx`

**Статус:** Используется статический JS для темы. Риск низкий, но лучше вынести в отдельный скрипт.

---

## ✅ ЧТО СДЕЛАНО ХОРОШО

1. **Аутентификация:** NextAuth.js, bcrypt, проверка сессий
2. **Авторизация:** Роли, `access-control.ts`, фильтрация по `companyId`
3. **Rate limiting:** Разные лимиты для API, webhooks, форм
4. **Шифрование:** AES-256-GCM для токенов, паролей, API-ключей
5. **Webhooks:** HMAC-SHA256, timing-safe сравнение
6. **SQL:** Prisma ORM, `$queryRawUnsafe` только с параметрами
7. **Security headers:** CSP, X-Frame-Options, HSTS и др.
8. **Веб-формы:** CORS, rate limit, honeypot, `sanitizeFormFields`
9. **Файлы:** Проверка доступа через `getDirectWhereCondition`, доступ только к своим сущностям
10. **`.env` в `.gitignore`:** Конфиденциальные данные не попадают в репозиторий

---

## 📋 ПЛАН ДЕЙСТВИЙ

| Приоритет | Задача | Сложность |
|-----------|--------|-----------|
| P0 | Исправить reset-password-emergency (обязательная проверка ключа) | Низкая |
| P0 | Добавить валидацию типов файлов при загрузке | Средняя |
| P1 | Санитизация HTML в шаблонах писем (DOMPurify) | Средняя |
| P2 | Ужесточить политику паролей (минимум 8 символов) | Низкая |
| P2 | Ограничить или упростить ответ `/api/health` | Низкая |

---

## 🔐 РЕКОМЕНДАЦИИ ПО НАСТРОЙКЕ

- Убедиться, что в production заданы: `ENCRYPTION_KEY`, `AUTH_SECRET`, `EMERGENCY_PASSWORD_RESET_KEY` (или эндпоинт отключён)
- В `docker-compose` сменить `POSTGRES_PASSWORD` по умолчанию (`change_this_password`)
- Запускать `npm audit` и обновлять зависимости
