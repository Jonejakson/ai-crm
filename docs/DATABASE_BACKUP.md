# Резервное копирование базы данных

## 📋 Обзор

Система автоматического резервного копирования PostgreSQL для Flame CRM.

## 🔧 Настройка

### 1. Ручной бэкап

```bash
cd /opt/flamecrm
./scripts/backup-db.sh
```

Бэкап будет сохранен в `/opt/flamecrm/backups/` с именем `crm_db_backup_YYYYMMDD_HHMMSS.sql.gz`

### 2. Автоматические бэкапы (через cron)

```bash
cd /opt/flamecrm
chmod +x scripts/backup-db.sh
chmod +x scripts/setup-backup-cron.sh
./scripts/setup-backup-cron.sh
```

Это добавит задачу в crontab для ежедневного бэкапа в 2:00 ночи.

### 3. Настройка расписания

По умолчанию бэкапы выполняются в 2:00 ночи каждый день. Чтобы изменить расписание:

```bash
# Редактировать crontab
crontab -e

# Или использовать переменную окружения
export CRON_SCHEDULE="0 3 * * *"  # В 3:00 ночи
./scripts/setup-backup-cron.sh
```

Формат cron: `минута час день месяц день_недели`

Примеры:
- `0 2 * * *` - каждый день в 2:00
- `0 */6 * * *` - каждые 6 часов
- `0 2 * * 0` - каждое воскресенье в 2:00

### 4. Настройка хранения бэкапов

По умолчанию бэкапы хранятся 7 дней. Чтобы изменить:

```bash
export RETENTION_DAYS=30  # Хранить 30 дней
./scripts/backup-db.sh
```

Или отредактируйте переменную в скрипте `scripts/backup-db.sh`:

```bash
RETENTION_DAYS="${RETENTION_DAYS:-7}"  # Измените 7 на нужное количество дней
```

### 5. Загрузка бэкапов в S3 (опционально)

Если настроен S3 (Selectel Object Storage), бэкапы автоматически загружаются в S3.

Требования:
- Установлен AWS CLI: `apt-get install awscli`
- Настроены переменные окружения S3 в `.env`

Бэкапы в S3 сохраняются в: `s3://ваш-бакет/backups/db/`

## 📥 Восстановление из бэкапа

### 1. Список доступных бэкапов

```bash
ls -lh /opt/flamecrm/backups/crm_db_backup_*.sql.gz
```

### 2. Восстановление

```bash
cd /opt/flamecrm
chmod +x scripts/restore-db.sh
./scripts/restore-db.sh /opt/flamecrm/backups/crm_db_backup_20250127_120000.sql.gz
```

**ВНИМАНИЕ:** Восстановление перезапишет текущую базу данных!

### 3. Восстановление из S3

Если бэкап в S3:

```bash
# Скачать из S3
aws s3 cp s3://ваш-бакет/backups/db/crm_db_backup_20250127_120000.sql.gz \
    /tmp/backup.sql.gz \
    --endpoint-url=https://s3.selcdn.ru \
    --region=ru-7

# Восстановить
./scripts/restore-db.sh /tmp/backup.sql.gz
```

## 📊 Мониторинг

### Проверка логов бэкапов

```bash
tail -f /opt/flamecrm/backups/backup.log
```

### Проверка статуса cron задач

```bash
crontab -l
```

### Проверка последних бэкапов

```bash
ls -lht /opt/flamecrm/backups/crm_db_backup_*.sql.gz | head -10
```

## 🔍 Устранение неполадок

### Бэкап не создается

1. Проверьте, что контейнер PostgreSQL запущен:
   ```bash
   docker-compose ps postgres
   ```

2. Проверьте логи:
   ```bash
   tail -50 /opt/flamecrm/backups/backup.log
   ```

3. Проверьте права доступа:
   ```bash
   ls -la /opt/flamecrm/backups/
   chmod 755 /opt/flamecrm/backups/
   ```

### Cron не работает

1. Проверьте, что cron запущен:
   ```bash
   systemctl status cron
   ```

2. Проверьте логи cron:
   ```bash
   tail -f /opt/flamecrm/backups/cron.log
   ```

3. Проверьте задачи cron:
   ```bash
   crontab -l
   ```

### Недостаточно места на диске

1. Проверьте использование диска:
   ```bash
   df -h
   ```

2. Удалите старые бэкапы вручную:
   ```bash
   find /opt/flamecrm/backups -name "crm_db_backup_*.sql.gz" -mtime +7 -delete
   ```

## 📝 Рекомендации

1. **Тестирование восстановления:** Регулярно тестируйте восстановление из бэкапов (минимум раз в месяц)

2. **Хранение в S3:** Настройте загрузку бэкапов в S3 для дополнительной защиты

3. **Мониторинг:** Настройте алерты при неудачных бэкапах

4. **Проверка размера:** Регулярно проверяйте размер бэкапов и доступное место на диске

## 🔒 Безопасность

- Бэкапы содержат все данные, включая пароли (зашифрованные)
- Храните бэкапы в безопасном месте
- Ограничьте доступ к директории с бэкапами:
  ```bash
  chmod 700 /opt/flamecrm/backups/
  ```

## 📞 Поддержка

При возникновении проблем проверьте:
- Логи бэкапов: `/opt/flamecrm/backups/backup.log`
- Логи cron: `/opt/flamecrm/backups/cron.log`
- Статус контейнеров: `docker-compose ps`

