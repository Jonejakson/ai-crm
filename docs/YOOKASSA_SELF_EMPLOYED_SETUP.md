# Настройка ЮKassa для самозанятого

## 📋 Пошаговая инструкция

### Шаг 1: Регистрация в ЮKassa

1. Перейдите на сайт: **https://yookassa.ru**
2. Нажмите кнопку **"Подключиться"** или **"Регистрация"**
3. Выберите тип регистрации: **"Самозанятый"** (или **"Индивидуальный предприниматель"**)
4. Заполните все необходимые данные:
   - ФИО
   - ИНН (ваш ИНН самозанятого)
   - Номер телефона
   - Email
   - Паспортные данные

### Шаг 2: Верификация аккаунта

1. После регистрации вам нужно будет пройти верификацию:
   - Загрузите скан паспорта (главная страница + прописка)
   - Подтвердите номер телефона (SMS)
   - Подтвердите email
2. Дождитесь проверки документов (обычно 1-3 рабочих дня)

### Шаг 3: Получение ключей API

1. Войдите в личный кабинет ЮKassa: **https://yookassa.ru/my**
2. Перейдите в раздел **"Настройки"** → **"API"**
3. Скопируйте следующие данные:
   - **Shop ID** (идентификатор магазина) - выглядит как `123456`
   - **Secret Key** (секретный ключ) - длинная строка вида `live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

⚠️ **ВАЖНО:** 
- Для тестирования используйте **тестовые ключи** (начинаются с `test_`)
- Для реальных платежей нужны **боевые ключи** (начинаются с `live_`)
- Тестовые ключи можно получить сразу после регистрации
- Боевые ключи доступны только после верификации

### Шаг 4: Настройка переменных окружения

#### На локальном компьютере (для разработки):

1. Откройте файл `.env` в корне проекта
2. Добавьте следующие строки:

```env
# ЮKassa (тестовые ключи для разработки)
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=test_ваш_секретный_ключ

# URL вашего приложения
NEXTAUTH_URL=http://localhost:3000
```

#### На сервере (для продакшена):

1. Подключитесь к серверу:
   ```bash
   ssh root@79.143.30.96
   ```

2. Перейдите в директорию проекта:
   ```bash
   cd /opt/flamecrm
   ```

3. Откройте файл `.env`:
   ```bash
   nano .env
   ```

4. Добавьте или обновите следующие строки:

```env
# ЮKassa (боевые ключи для продакшена)
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=live_ваш_секретный_ключ

# URL вашего домена (замените на ваш реальный домен)
NEXTAUTH_URL=https://ваш-домен.ru

# Отключите режим разработки
DEV_MODE=false
NODE_ENV=production
```

5. Сохраните файл (Ctrl+O, Enter, Ctrl+X)

6. Перезапустите приложение:
   ```bash
   docker-compose restart app
   ```

### Шаг 5: Настройка Webhook

Webhook нужен для автоматического обновления статуса платежей в вашей системе.

1. В личном кабинете ЮKassa перейдите в **"Настройки"** → **"Уведомления"**
2. Нажмите **"Добавить URL"**
3. Введите URL вашего webhook:
   ```
   https://ваш-домен.ru/api/billing/webhook
   ```
   
   **Пример:**
   - Если ваш домен: `crm.example.com`
   - То URL будет: `https://crm.example.com/api/billing/webhook`
   
   **Важно:** 
   - URL должен быть доступен по HTTPS
   - Домен должен быть подтвержден в ЮKassa

4. Выберите события для отправки:
   - ✅ **payment.succeeded** (обязательно) - успешная оплата
   - ✅ **payment.canceled** (рекомендуется) - отмена платежа
   - ✅ **payment.waiting_for_capture** (опционально) - ожидание подтверждения

5. Сохраните настройки

### Шаг 6: Тестирование

#### Тестовый режим (до верификации):

1. Используйте **тестовые ключи** в `.env`:
   ```env
   YOOKASSA_SHOP_ID=ваш_тестовый_shop_id
   YOOKASSA_SECRET_KEY=test_ваш_тестовый_ключ
   ```

2. Тестовые карты для проверки:
   - **Успешная оплата:** `5555 5555 5555 4444`
     - Срок действия: любая будущая дата (например, 12/25)
     - CVV: любые 3 цифры (например, 123)
   - **Отклоненная оплата:** `5555 5555 5555 4477`

3. Проверка работы:
   - Зайдите в вашу CRM систему
   - Перейдите в раздел **"Компания"** → **"Управление подпиской"**
   - Выберите платный тариф
   - Будет создан платеж и перенаправление на страницу оплаты ЮKassa
   - Используйте тестовую карту для оплаты
   - После успешной оплаты вы будете перенаправлены на страницу успеха
   - Webhook автоматически активирует подписку

#### Боевой режим (после верификации):

1. Замените тестовые ключи на боевые в `.env`:
   ```env
   YOOKASSA_SHOP_ID=ваш_боевой_shop_id
   YOOKASSA_SECRET_KEY=live_ваш_боевой_ключ
   ```

2. В личном кабинете ЮKassa переключите магазин из **тестового режима** в **боевой**

3. Перезапустите приложение на сервере:
   ```bash
   docker-compose restart app
   ```

4. Проведите тестовый платеж на небольшую сумму (например, 1 рубль)

### Шаг 7: Проверка безопасности

⚠️ **КРИТИЧЕСКИ ВАЖНО:** В продакшене обязательно должна быть включена проверка подписи webhook!

1. Проверьте, что в `.env` установлен `NODE_ENV=production`
2. Проверьте, что `YOOKASSA_SECRET_KEY` установлен (не пустой)
3. Проверка подписи автоматически включится в production режиме

### Шаг 8: Мониторинг платежей

1. В личном кабинете ЮKassa перейдите в **"Платежи"** для просмотра всех транзакций
2. В вашей CRM системе все счета сохраняются в базе данных
3. Проверить статус счета можно через API: `GET /api/billing/invoice/[id]`

## 🔧 Устранение проблем

### Проблема: "Payment system not configured"

**Решение:**
- Проверьте, что в `.env` файле установлены `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY`
- Убедитесь, что нет лишних пробелов в значениях
- Перезапустите приложение после изменения `.env`

### Проблема: Webhook не работает

**Решение:**
1. Проверьте, что URL webhook доступен по HTTPS
2. Проверьте логи приложения:
   ```bash
   docker-compose logs -f app
   ```
3. В личном кабинете ЮKassa проверьте историю отправки webhook'ов
4. Убедитесь, что домен подтвержден в ЮKassa

### Проблема: Платеж создается, но не проходит

**Решение:**
1. Проверьте, что используете правильные ключи (тестовые для теста, боевые для продакшена)
2. Проверьте баланс в ЮKassa (должен быть положительный)
3. Проверьте лимиты на счете самозанятого
4. Убедитесь, что верификация пройдена (для боевых платежей)

### Проблема: "Invalid signature" в webhook

**Решение:**
1. Убедитесь, что `YOOKASSA_SECRET_KEY` правильный
2. Проверьте, что используется тот же ключ, что и в личном кабинете ЮKassa
3. Перезапустите приложение после изменения ключа

## 📊 Структура платежей в системе

- **Создание платежа:** `POST /api/billing/payment`
- **Webhook (обработка уведомлений):** `POST /api/billing/webhook`
- **Проверка счета:** `GET /api/billing/invoice/[id]`
- **Страница успеха:** `/billing/success?invoiceId=...`
- **Страница отмены:** `/billing/cancel?invoiceId=...`

## 💰 Комиссии ЮKassa для самозанятых

- **Банковские карты:** 2.8% + 20₽ за операцию
- **Электронные кошельки:** 3.5% + 20₽ за операцию
- **СБП (Система быстрых платежей):** 0.5% + 20₽ за операцию

**Важно:** Комиссия списывается автоматически при каждом платеже.

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи приложения: `docker-compose logs -f app`
2. Проверьте настройки webhook в личном кабинете ЮKassa
3. Обратитесь в поддержку ЮKassa: https://yookassa.ru/developers/support
4. Проверьте документацию: https://yookassa.ru/developers/api

## ✅ Чек-лист готовности

- [ ] Регистрация в ЮKassa завершена
- [ ] Верификация пройдена (для боевых платежей)
- [ ] Получены ключи API (Shop ID и Secret Key)
- [ ] Ключи добавлены в `.env` файл (локально и на сервере)
- [ ] Webhook настроен в личном кабинете ЮKassa
- [ ] Тестовый платеж успешно проведен
- [ ] Проверка подписи webhook включена (для продакшена)
- [ ] Приложение перезапущено после изменения настроек
- [ ] Боевой режим активирован (после верификации)

---

**Готово!** Теперь ваша CRM система готова принимать платежи через ЮKassa! 🎉

